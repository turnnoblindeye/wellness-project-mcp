[
  {
    "name": "log_meal",
    "description": "Log a meal to the user's food diary. Use when the user mentions eating, describes a meal, or asks to log food.\n\nINFER:\n- date: today, or from context (\"yesterday\", \"last night\")\n- meal_type: canonical time mapping (00-05 Snack, 05-10 Breakfast, 10-14 Lunch, 14-17 Snack, 17-22 Dinner, 22-24 Snack); context overrides (\"post-workout shake\"=Post-Workout)\n\nMACRO SOURCE: strongest evidence wins. Never replace known stored macros with a fresh estimate.\n1. REPEATS: if the user refers to a previously logged item (\"same\", \"another\", \"more\", \"again\", or equivalent in any language), call list_meals for the referenced date/range first. If one row unambiguously matches, reuse its stored calories/protein_g/fat_g/carbs_g/alcohol_g and scale by the quantity ratio when the row's quantity is known. If the match or quantity is ambiguous, ask instead of re-estimating. If nothing matches, continue below.\n2. SAVED RECIPES: if the user says the food is saved/usual, pass recipe_name. It supplies stored food and macros; explicit fields override them. Relay no-match/ambiguous errors instead of guessing. If the recipe reports missing macros, estimate only those fields and retry. If food_items adds food beyond the recipe, pass all four macros as the combined total.\n3. BRAND NAMES: for a branded, restaurant, or specific product, use published macros for the stated size/variant before a generic estimate.\n4. Otherwise estimate calories, protein_g, fat_g, carbs_g from the food description. Never ask the user for macros.\n\nMACROS: food_items plus calories/protein_g/fat_g/carbs_g are required unless recipe_name supplies them. Ask only if food_items are absent and no recipe_name applies. Never call without all four macros populated.\n\nFASTING: if the user ate nothing / fasted all day, log one \"Fast day\" Snack with calories/protein_g/fat_g/carbs_g all 0.\n\nDUPLICATES: if this tool returns a duplicate error, tell the user what's already logged and ask whether this is a separate serving (retry with force=true) or should update the existing entry instead (update_meal with adjusted values).\n\nDRINKS / HYDRATION: this is the single write path for consumed drinks too. When a drink amount is known or reasonably inferable, include it in fluids even when the same drink also contributes calories/macros/alcohol. The server decides whether hydration tracking is enabled; never use a separate hydration write tool. A plain fluid-only intake may omit food_items and macros and send only fluids. For alcoholic drinks, include that drink's alcohol_g in its fluid item; if there is exactly one drink, the top-level alcohol_g can stand in.",
    "annotations": {
      "title": "Log a meal",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "YYYY-MM-DD. Omit for today (the server resolves it in the user's own timezone, more reliable than guessing). Send explicitly for any past date."
        },
        "meal_type": {
          "type": "string",
          "enum": [
            "Breakfast",
            "Lunch",
            "Dinner",
            "Snack",
            "Post-Workout",
            "Shake",
            "Other"
          ],
          "enumFallback": {
            "value": "Other",
            "annotate": "food_items"
          },
          "description": "Required. Infer from time of day or context, even when recipe_name is used (a saved recipe's own stored meal type never fills this in)."
        },
        "recipe_name": {
          "type": "string",
          "description": "Name or close phrase for one of the user's saved recipes, e.g. \"protein oats\", \"chicken bowl\", or \"breakfast\" for their saved Breakfast recipe. See SAVED RECIPES above."
        },
        "food_items": {
          "type": "string",
          "description": "Description of the food and drinks consumed. See MACROS above; ask the user only if completely absent and no recipe_name applies."
        },
        "calories": {
          "type": "integer",
          "description": "Total calories (kcal). See MACROS above."
        },
        "protein_g": {
          "type": "number",
          "description": "Protein in grams. See MACROS above."
        },
        "fat_g": {
          "type": "number",
          "description": "Fat in grams. See MACROS above."
        },
        "carbs_g": {
          "type": "number",
          "description": "Carbohydrates in grams. See MACROS above."
        },
        "alcohol_g": {
          "type": "number",
          "description": "Alcohol in grams (not kcal). Only if alcoholic drinks were consumed; unset takes the saved recipe's value when recipe_name matches. 1 standard drink is about 14g."
        },
        "force": {
          "type": "boolean",
          "description": "True only when the user has explicitly confirmed a separate entry despite a duplicate warning. Bypasses duplicate detection."
        },
        "fluids": {
          "type": "array",
          "description": "Optional drinks consumed in this intake. Omit when no drink amount is known. Hydration is persisted only when the user enabled hydration tracking.",
          "items": {
            "type": "object",
            "properties": {
              "volume": {
                "type": "number",
                "description": "Consumed drink volume in the supplied unit."
              },
              "unit": {
                "type": "string",
                "enum": [
                  "ml",
                  "fl_oz"
                ],
                "description": "Volume unit. Default ml."
              },
              "beverage_type": {
                "type": "string",
                "enum": [
                  "water",
                  "electrolyte",
                  "sports_drink",
                  "coffee",
                  "tea",
                  "juice",
                  "milk",
                  "soda",
                  "broth",
                  "other"
                ],
                "description": "Broad drink type. Default water. Use other when the exact drink is not in the enum and preserve its name in beverage_name."
              },
              "beverage_name": {
                "type": "string",
                "description": "Specific drink name when useful, e.g. IPA, vodka soda, latte."
              },
              "alcohol_g": {
                "type": "number",
                "description": "Alcohol grams in this specific drink. Required per alcoholic fluid when more than one drink is present."
              }
            },
            "required": [
              "volume"
            ]
          }
        }
      },
      "required": [
        "meal_type"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_meals",
    "description": "List all meals logged for a date or date range, including each meal's ID, date, type, food description, and macros. update_meal and delete_meal can resolve a meal on their own from date (+ optional name substring), so this is no longer required before either — use it to answer \"what did I eat today/this week/yesterday?\", review what has been logged, or get an id after an ambiguous update_meal/delete_meal match.\n\nMaximum range: 31 days per call. For longer periods, make multiple calls with sequential date ranges.\n\nINFER — do not ask:\n- date: default to today\n- end_date: if the user asks about a week or range, set end_date to cover the full period (e.g. \"this week\" → date=Monday, end_date=today; \"last 7 days\" → date=7 days ago, end_date=today). For a single day, omit end_date.",
    "annotations": {
      "title": "List meals",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "Start date (or the single date if no range). Format: YYYY-MM-DD. Optional — omit for today (resolved in the user's own timezone)."
        },
        "end_date": {
          "type": "string",
          "description": "End date for a range query. Format: YYYY-MM-DD. Optional — omit for a single-day lookup."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_meal",
    "description": "Update an existing meal. Use only when the current message explicitly changes, corrects, or adds to a meal already logged. Never infer an update from earlier chat history. A plain food statement (\"coffee with milk\") is a new entry: use log_meal, even if that meal type already exists today.\n\nFIND THE MEAL: use id if known. Otherwise use date (YYYY-MM-DD, defaults to today) plus name and/or target_meal_type to narrow the existing row. target_meal_type finds the current type and is never written; meal_type sets a new type. If the match is not exactly one row, nothing changes. On multiple matches, ask the user which meal they mean; never select a candidate id yourself. Send only fields that change.\n\nTHREE MODES. Picking the wrong one corrupts the meal.\n\n1. ADD SAVED FOOD -> add_recipe_name. \"add my usual kombucha to breakfast\", \"add the protein shake I saved to lunch\". Do NOT use log_meal for this, that logs a second, separate meal. Additive: the recipe's food text is appended to the meal's existing food_items and each of its macros is ADDED to the meal's current value for that field, so the result is both foods together with both foods' calories, not a replacement. Matched the same way as log_meal's recipe_name (case-insensitive exact title, then substring); no match or more than one substring match throws an error naming the candidates or the user's saved titles instead of guessing. If the matched recipe has no stored value for one of calories/protein_g/fat_g/carbs_g, this throws carrying the recipe's food text: estimate just the missing field(s) and call again with the same id (or date+name) and add_recipe_name plus those field(s) set explicitly. An explicit field passed alongside add_recipe_name takes the recipe's place for that one field and is still ADDED to the meal; it does not overwrite the meal's total the way it does without add_recipe_name.\n\n2. ADD UNSAVED FOOD -> add_food_items. \"add a banana to my breakfast\", \"I also had a small coffee\", where the extra food is not one of the user's saved recipes. Pass the new food's own description plus calories/protein_g/fat_g/carbs_g estimated for JUST that new food, not the meal's new total and not the existing food's macros. All four are required whenever add_food_items is set (there is no recipe to fall back on): estimate them from the description, never ask the user. Additive exactly like add_recipe_name: add_food_items is appended to the existing food_items (unless food_items is ALSO passed explicitly, which replaces the description outright instead of appending) and each macro is ADDED to the meal's current value; alcohol_g is optional and adds nothing when omitted.\nadd_food_items and add_recipe_name are mutually exclusive: never set both in the same call. Adding both a saved recipe and separate ad-hoc food means two update_meal calls, one per addition.\n\n3. CORRECT A VALUE -> plain fields, both add_* unset. \"that was 400 calories, not 600\", \"actually it was just eggs, no toast\", \"make it 500 calories\": the meal's stored total needs to become a specific NEW number, not grow. Each of food_items/calories/protein_g/fat_g/carbs_g/alcohol_g you pass REPLACES the meal's current value for that field outright, so send the corrected TOTAL for that field, never an amount to add.\n\nReaching for mode 3 when the meal is GROWING is the exact bug this tool used to have: it silently REPLACES the whole meal with just the new food and throws away what was already logged.\n\nIf add-vs-correct intent is ambiguous, ask before updating.\n\nDRINKS / HYDRATION WHEN UPDATING: a food update must not silently erase an already-linked hydration event. For changes unrelated to drinks, omit fluids and the existing hydration is preserved. When ADDING a drink with add_food_items/add_recipe_name, fluids contains only the newly added drink(s) and they are appended to the meal's hydration. When CORRECTING the meal's drinks with both add_* fields omitted, fluids is the complete corrected drink list and replaces the linked hydration only after replacement rows have been safely inserted. If the correction removes every drink, explicitly send fluids: [] and the linked hydration is deleted. Whenever a drink volume is known or reasonably inferable, include it.",
    "annotations": {
      "title": "Update a meal",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Meal ID, if already known. Alternative to date + name/target_meal_type, see FIND THE MEAL above."
        },
        "date": {
          "type": "string",
          "description": "Date the meal was logged. Format: YYYY-MM-DD. Used with name and/or target_meal_type to find the meal when id is omitted; defaults to today if id and date are both omitted."
        },
        "name": {
          "type": "string",
          "description": "Substring of the food description (case-insensitive) to disambiguate multiple meals on the same date. Only used when id is omitted."
        },
        "target_meal_type": {
          "type": "string",
          "enum": [
            "Breakfast",
            "Lunch",
            "Dinner",
            "Snack",
            "Post-Workout",
            "Shake",
            "Other"
          ],
          "description": "Which meal type to FIND on the date, e.g. Breakfast, to disambiguate multiple meals logged that day -- \"update today's breakfast\" is target_meal_type: \"Breakfast\". Case-insensitive, only used when id is omitted. This is NEVER written to the meal; it only narrows the search, exactly like name above. Distinct from meal_type below, which SETS the new type to write. If no meal of this type is logged on the date, the call throws naming the meal types that ARE logged that day and changes nothing -- it never falls back to whichever meal the date happens to match."
        },
        "meal_type": {
          "type": "string",
          "enum": [
            "Breakfast",
            "Lunch",
            "Dinner",
            "Snack",
            "Post-Workout",
            "Shake",
            "Other"
          ],
          "description": "Updated meal type to WRITE onto the meal (e.g. reclassify a Snack as Dinner). Optional, omit if not changing. Never inferred from add_recipe_name. Distinct from target_meal_type above, which FINDS a meal by its current type and is never written."
        },
        "add_recipe_name": {
          "type": "string",
          "description": "Optional. Name (or a close phrase) of one of the user's saved recipes to FOLD INTO this meal, e.g. \"kombucha\", \"protein shake\" (see FOLD IN A SAVED RECIPE above). Adds the recipe's food text and macros onto the meal's current values; never use this to replace a meal outright (omit it and pass food_items/calories/etc. directly for that). Distinct from log_meal's recipe_name, which SETS a new meal's fields instead of adding to one that already exists. Mutually exclusive with add_food_items -- never set both."
        },
        "add_food_items": {
          "type": "string",
          "description": "Optional. Description of food to ADD to this meal that ISN'T a saved recipe, e.g. \"a banana\", \"small coffee with cream\" (see ADD FOOD THAT WASN'T SAVED above). Requires calories/protein_g/fat_g/carbs_g to be set to the estimated macros of just this new food -- never the meal's new total. Appends this text onto the meal's existing food_items and adds the macro fields onto the meal's current values; never use this to replace a meal outright (omit it and pass food_items/calories/etc. directly for that). Mutually exclusive with add_recipe_name -- never set both; if adding a saved recipe and separate ad-hoc food, make two calls."
        },
        "food_items": {
          "type": "string",
          "description": "Updated food description. Optional, omit if not changing. With add_recipe_name and add_food_items both omitted, this REPLACES the current description outright. With either one present and this omitted, the new food's text (the recipe's stored food_items, or add_food_items itself) is appended instead. Passing this alongside add_recipe_name/add_food_items overrides the append with this exact text."
        },
        "calories": {
          "type": "integer",
          "description": "Updated total calories (kcal). Optional, omit if not changing (or, with add_recipe_name, if the recipe already has a stored value). REPLACES the current value unless add_recipe_name or add_food_items is also set, in which case this is the AMOUNT BEING ADDED (the new food's own calories, not the meal's new total), added onto the meal's current value. Required whenever add_food_items is set, since there is no saved recipe to fall back on."
        },
        "protein_g": {
          "type": "number",
          "description": "Updated protein in grams. Same REPLACE-vs-ADD rule as calories above, including required-with-add_food_items."
        },
        "fat_g": {
          "type": "number",
          "description": "Updated fat in grams. Same REPLACE-vs-ADD rule as calories above, including required-with-add_food_items."
        },
        "carbs_g": {
          "type": "number",
          "description": "Updated carbohydrates in grams. Same REPLACE-vs-ADD rule as calories above, including required-with-add_food_items."
        },
        "alcohol_g": {
          "type": "number",
          "description": "Updated alcohol in grams. Same REPLACE-vs-ADD rule as calories above, except this one stays optional even with add_recipe_name or add_food_items set: omitting it just adds nothing."
        },
        "fluids": {
          "type": "array",
          "description": "Optional drinks consumed in this intake. Omit when no drink amount is known. Hydration is persisted only when the user enabled hydration tracking.",
          "items": {
            "type": "object",
            "properties": {
              "volume": {
                "type": "number",
                "description": "Consumed drink volume in the supplied unit."
              },
              "unit": {
                "type": "string",
                "enum": [
                  "ml",
                  "fl_oz"
                ],
                "description": "Volume unit. Default ml."
              },
              "beverage_type": {
                "type": "string",
                "enum": [
                  "water",
                  "electrolyte",
                  "sports_drink",
                  "coffee",
                  "tea",
                  "juice",
                  "milk",
                  "soda",
                  "broth",
                  "other"
                ],
                "description": "Broad drink type. Default water. Use other when the exact drink is not in the enum and preserve its name in beverage_name."
              },
              "beverage_name": {
                "type": "string",
                "description": "Specific drink name when useful, e.g. IPA, vodka soda, latte."
              },
              "alcohol_g": {
                "type": "number",
                "description": "Alcohol grams in this specific drink. Required per alcoholic fluid when more than one drink is present."
              }
            },
            "required": [
              "volume"
            ]
          }
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_meal",
    "description": "Permanently delete a meal entry. Use when the user explicitly asks to remove or delete a logged meal.\n\nFIND THE MEAL: pass id if already known. Otherwise pass date (YYYY-MM-DD, defaults to today) and, only if more than one meal was logged that day, name (a substring of the food description, case-insensitive) to narrow it down. This action is irreversible — a match that isn't exactly one meal returns an error explaining why, with nothing deleted; retry with id or a narrower name, never guess.\n\nHYDRATION: any assistant hydration events linked to this meal are deleted by the database in the same food-row delete. Do not issue a separate hydration delete.",
    "annotations": {
      "title": "Delete a meal",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Meal ID, if already known. Alternative to date + name — see FIND THE MEAL above."
        },
        "date": {
          "type": "string",
          "description": "Date the meal was logged. Format: YYYY-MM-DD. Used with name to find the meal when id is omitted; defaults to today if id and date are both omitted."
        },
        "name": {
          "type": "string",
          "description": "Substring of the food description (case-insensitive) to disambiguate multiple meals on the same date. Only used when id is omitted."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "mark_empty_day",
    "description": "Set, change, or undo the answer to \"why is this day empty?\" for a date with no real meals logged. Use when the user wants to flip a fast day to forgotten (or back), or undo either one, in chat instead of the in-app prompt.\n\nThere are exactly three states for a date, and this tool is the only way to move between them:\n- fast: writes the 0-kcal \"Fast day\" food_log entry (a real, counted 0-calorie day).\n- forgot: records that the day was reviewed and simply not logged. Writes nothing to food_log, so the day stays a true blank and is excluded from calorie/TDEE averages, never imputed as 0.\n- unanswered: clears both. The day goes back to being an open question and the in-app prompt may ask about it again.\n\nSetting one answer always clears the other, so a date is never both a fast and a forgotten day at once.\n\nUse list_meals first if unsure whether the date already has real food logged. This tool refuses to touch a day that has actual meals on it (other than an existing fast marker).",
    "annotations": {
      "title": "Mark an empty day",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "The date being answered for. Format: YYYY-MM-DD. Required."
        },
        "answer": {
          "type": "string",
          "enum": [
            "fast",
            "forgot",
            "unanswered"
          ],
          "description": "Required. \"fast\" = intentional 0-calorie day. \"forgot\" = day stays excluded, not imputed as 0. \"unanswered\" = clear any prior answer."
        }
      },
      "required": [
        "date",
        "answer"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_hydration",
    "description": "Review hydration events and stored effective hydration totals. Use when the user explicitly asks about hydration history or fluid intake. Hydration tracking must already be enabled in Settings. Maximum range 31 days. Defaults to the last 7 days.",
    "annotations": {
      "title": "List hydration",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start date in YYYY-MM-DD format. Default 6 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End date in YYYY-MM-DD format. Default today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_exercises",
    "description": "Returns all canonical exercise names from the exercise library, grouped by muscle group. Call this before log_workout or update_workout to match user-described exercise names to canonical ones. Canonical names ensure proper exercise tracking and NSI score calculation.",
    "annotations": {
      "title": "List exercises",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_workout",
    "description": "UNIT INPUTS: never convert units yourself. Pass the user's number for a _lb / _mi / _in field exactly as stated, and when they gave kg / km / cm set input_weight_unit / input_distance_unit / input_length_unit in that same object. Omit the companion when the number is already lb / mi / in. The tool converts once before storage, so this overrides any wording that asks you to do the arithmetic.\n\nLog a complete workout session: exercises, sets, reps, weights, and session metadata. Use when the user describes finishing a workout, lists exercises performed, or asks to log training. A workout they have not done yet is not a log: use propose_workout for that.\n\nEXERCISE NAMES:\n- Call list_exercises first; match each exercise to the closest canonical name. No reasonable match → use the name as stated. Don't ask before logging, match silently and log.\n- \"Chest press\" (machine) and \"bench press\" (barbell) are DISTINCT — pass the user's term through so the resolver's aliases pin the right one.\n- name is ONLY the exercise name, never reps/weights/sets — those go in the sets array.\n- LITERAL NAME: literal_name: true keeps the user's exact wording instead of the closest library match, skips the resolver, and gets no NSI score (no benchmark to compare an unmatched name against). Use for \"call it exactly X\", \"not the standard one\", \"literally X\", or a rejected match.\n- The result says when a name was matched to something other than what the user said. Relay it in your own words rather than repeating the line verbatim. If a name matches nothing closely enough, the result names near-miss library exercises; ask the user which they meant rather than accept the unscored custom log silently.\n- EQUIPMENT (load basis): dumbbell_pair is one dumbbell in EACH hand, weight_lb PER HAND (2x for NSI); dumbbell_single is one implement total. Laterality (single-leg/arm) does NOT decide this alone. Set it when the user describes the load (each hand, machine, band); a wrong or missing tag silently halves or doubles NSI. Values: barbell, dumbbell_pair, dumbbell_single, machine, kettlebell, bodyweight, band, cable, trx, other.\n\nSETS:\n- \"3 sets of 15 reps\" → 3 set objects with reps: 15. \"15/12/10\" → 3 sets with reps 15, 12, 10.\n- Pure isometric holds (planks, dead hangs, wall sits) have no reps: \"30 second plank\" = { hold_length_sec: 30 }.\n- Tempo/pause work combines reps + weight_lb + hold_length_sec (seconds per rep) on the same set, never in notes.\n- Loaded carries (farmers carry, sled push, weighted plank) are one set per trip: hold_length_sec + weight_lb, omit reps unless a trip count is given. weight_lb is PER HAND for a two-implement carry, TOTAL for one implement. Distance has no column and is never a duration — put it in notes.\n\nINFER — do not ask:\n- date: today, or from context\n- focus_type: from the exercises (bench/shoulders/triceps=Push, rows/pulldowns/curls=Pull, squats/deadlifts/lunges=Legs, mixed upper=Upper, everything=Full Body)\n- is_bodyweight: true for pull-ups, push-ups, dips, bodyweight squats\n- superset_group: same integer for exercises done back-to-back or as a superset\n- slot_type: 'warmup' for prep at the start, 'finisher' for burnout/cardio at the end, 'working' (default) otherwise\n\nRPE (Rate of Perceived Exertion) — 1-10 scale, half steps allowed (7.5, 8.5):\n- Session-level RPE: overall session difficulty. Infer from user comments like \"brutal session\" (8-9) or \"easy day\" (3-4). Optional.\n- Per-set RPE: how hard each individual set felt. Include only if the user explicitly mentions per-set effort or failure. Optional.\n- Scale: 1=minimal effort, 5=moderate, 7=hard, 8=few reps left, 9=one rep left, 10=maximal/failure.\n\nASK (single batched question) only if missing and not inferable: location, focus_type (list ambiguous), heart_points (tracker provides them but not mentioned).\n\nRETURNS the new session's ID (as \"[ID NN]\"). Pass it to update_workout / delete_workout / get_workout / add_exercises for follow-ups in this conversation.\n\nSAVED WORKOUT MODE: when the user asks to save a workout for reuse, set save_as_saved_workout=true and pass the full prescription through this same tool. In saved mode the workout is NOT logged as completed history. To replace an existing Saved Workout, also pass saved_workout_id; the supplied prescription fully replaces its prior prescribed sets/reps/weights. Use saved_workout_title when the reusable name should differ from focus_type.",
    "annotations": {
      "title": "Log a workout",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "YYYY-MM-DD. Default to today."
        },
        "focus_type": {
          "type": "string",
          "description": "Infer from the exercises: Push, Pull, Legs, Upper, Lower, Full Body, Cardio, Mobility. Ask only if genuinely unclear."
        },
        "location": {
          "type": "string",
          "description": "Gym, Home, Outdoor. Infer from context or ask."
        },
        "heart_points_moderate": {
          "type": "integer",
          "description": "Moderate-intensity heart points, Google Fit or equivalent, if mentioned."
        },
        "heart_points_peak": {
          "type": "integer",
          "description": "Peak-intensity heart points, if mentioned."
        },
        "notes": {
          "type": "string",
          "description": "Session notes: how it went, PRs hit, how they felt."
        },
        "rpe": {
          "type": "number",
          "description": "Session RPE, 1-10, half steps allowed: 5 moderate, 7 hard, 9 one rep left, 10 failure. Infer from comments about overall difficulty, or omit."
        },
        "exercises": {
          "type": "array",
          "description": "Every exercise performed, in order.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Canonical name from list_exercises if one matches, else the name as stated."
              },
              "literal_name": {
                "type": "boolean",
                "description": "Keep exactly as given, skipping the library match. See LITERAL NAME above. Default false."
              },
              "superset_group": {
                "type": "integer",
                "description": "Same positive integer for exercises done back-to-back as a superset. Omit for standalone."
              },
              "slot_type": {
                "type": "string",
                "enum": [
                  "warmup",
                  "working",
                  "finisher"
                ],
                "description": "warmup for prep at the start, finisher for burnout/cardio at the end, working otherwise."
              },
              "equipment": {
                "type": "string",
                "enum": [
                  "barbell",
                  "dumbbell_pair",
                  "dumbbell_single",
                  "machine",
                  "kettlebell",
                  "bodyweight",
                  "band",
                  "cable",
                  "trx",
                  "other"
                ],
                "description": "Load basis for every set of this exercise. See EQUIPMENT above. Optional; overrides the auto guess."
              },
              "sets": {
                "type": "array",
                "description": "Every set for this exercise, in order.",
                "items": {
                  "type": "object",
                  "properties": {
                    "reps": {
                      "type": "integer",
                      "description": "Reps this set. Omit for a pure hold. Can coexist with hold_length_sec and weight_lb."
                    },
                    "weight_lb": {
                      "type": "number",
                      "description": "Load. For a band, the band label weight (e.g. 80 for an 80lb band). Omit for bodyweight. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
                    },
                    "is_bodyweight": {
                      "type": "boolean",
                      "description": "True if no external weight. Infer for pull-ups, push-ups, dips."
                    },
                    "is_band": {
                      "type": "boolean",
                      "description": "True for a band, not a cable or free weight; NSI scores band load differently. Infer from \"band face pull\", \"80lb band\"."
                    },
                    "band_label_lb": {
                      "type": "number",
                      "description": "Band label weight (e.g. 80 for an 80lb band). Use when is_band is true; defaults to weight_lb. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
                    },
                    "hold_length_sec": {
                      "type": "integer",
                      "description": "Seconds: a timed hold, seconds-per-rep for tempo work, or one carry trip. See SETS above."
                    },
                    "rpe": {
                      "type": "number",
                      "description": "RPE for this one set, same 1-10 scale as the session rpe field. Only when the user gives per-set effort."
                    },
                    "notes": {
                      "type": "string",
                      "description": "Per-set notes, e.g. \"failed last rep\", \"paused at bottom\", carry distance \"50 ft\"."
                    },
                    "input_weight_unit": {
                      "type": "string",
                      "enum": [
                        "lb",
                        "kg"
                      ],
                      "description": "Set to kg when the user gave kg for the _lb fields in this object. Omit when they are already lb."
                    }
                  }
                }
              }
            },
            "required": [
              "name",
              "sets"
            ]
          }
        },
        "save_as_saved_workout": {
          "type": "boolean",
          "description": "True when this payload is a reusable Saved Workout prescription, not a completed workout. Defaults to false."
        },
        "saved_workout_id": {
          "type": "integer",
          "description": "Existing Saved Workout ID to replace in saved mode. Omit to create a new Saved Workout."
        },
        "saved_workout_title": {
          "type": "string",
          "description": "Optional reusable workout name in saved mode. Defaults to focus_type."
        }
      },
      "required": [
        "date",
        "exercises"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_workouts",
    "description": "List workout sessions in a date range: ID, date, focus type, location, and session-level NSI with rating. Use before get_workout to find a session ID, or to answer \"how many times did I train this week?\", \"when was my last leg day?\", \"did I work out yesterday?\", \"how is my NSI trending?\".\n\nEach row's NSI is the mean of per-exercise NSIs (after dropping anything below 50% of the user's median for that exercise), with a rating band (Below Average, Novice, Average, Intermediate, Advanced, Elite). 100 = the population intermediate standard for the user's bodyweight, age, and sex. Use the rolling average across rows for trend questions.\n\nMaximum range: 90 days per call. For longer periods (PR lookups, \"have I ever done X\", \"when was the last time I did Y\"), make multiple sequential calls walking backwards (days 0-89, then 90-179, then 180-269...) until you find what you need. Don't give up after one call.\n\nINFER — default start_date to 7 days ago, end_date to today. Widen up to 90 days for trend questions. Chain calls for anything older.\n\nSAVED WORKOUTS: set saved_workouts=true to list the user's reusable Saved Workouts library instead of completed workout history. Saved workout IDs are separate from workout session IDs.",
    "annotations": {
      "title": "List workouts",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Optional — defaults to 7 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Optional — defaults to today."
        },
        "saved_workouts": {
          "type": "boolean",
          "description": "When true, list reusable Saved Workouts instead of completed workout sessions. Defaults to false."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "get_workout",
    "description": "Retrieve full detail of a workout session: exercises, sets, reps, weights, superset groupings, heart points, notes, and NSI scoring at every grain. Use for detailed questions about a past workout, reviewing training before recommendations, confirming what was logged, or comparing a session to population strength standards.\n\nNSI: session NSI/rating in the header; per-exercise NSI (max set NSI), rating, est. 1RM, and the population_1rm_lb/population_reps benchmark it was measured against; per-set NSI and est. 1RM to see which set drove the exercise score.\n\nEQUIPMENT: shown per exercise when every set shares a tag, else per set; missing means untagged. A wrong or missing tag on a dumbbell exercise silently halves or doubles its NSI score — fix it via update_workout's set_updates or add_exercises equipment field.\n\nREQUIRED WORKFLOW: call list_workouts first to find the session ID — never guess it.\n\nSAVED WORKOUTS: pass saved_workout_id to read a reusable Saved Workout prescription. Do not combine it with session_id/session_date.",
    "annotations": {
      "title": "Get workout detail",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "integer",
          "description": "Session ID from list_workouts. Required."
        },
        "saved_workout_id": {
          "type": "integer",
          "description": "Saved Workout ID from list_workouts(saved_workouts=true). When present, returns the reusable prescription instead of a completed workout session."
        }
      },
      "required": [
        "session_id"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "get_exercise_history",
    "description": "Look up everything the user has done for ONE exercise: all-time PR plus recent performance, across many sessions.\n\nUSE FOR:\n- PR lookups — \"what's my bench PR?\", \"have I ever squatted 315?\". Returns the est. 1RM PR and the exact set it came from (date, weight, reps, RPE, banded vs unbanded, superset siblings, notes), plus rep-range bests (1RM/3RM/5RM/10RM). Banded and unbanded PRs are shown side-by-side when both exist.\n- Recent-activity questions — \"how has my squat been lately?\", \"when did I last deadlift?\". Returns the most recent N sessions containing the exercise, formatted like get_workout.\n- Trend questions — \"am I getting stronger on incline DB press?\". Includes a one-line delta of current best vs ~30-90 days ago.\n\nNOT for a full session (every exercise in one workout — use get_workout) or a date-window list regardless of exercise (use list_workouts).\n\nINFER — do not ask: exercise_name (take the user's words; resolves to canonical, or says so if never logged), recent_limit (default 10 sessions), since_date (optional — narrows only the Recent block; the PR is always all-time).",
    "annotations": {
      "title": "Exercise history",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "exercise_name": {
          "type": "string",
          "description": "Exercise to look up. Required. Free-text — the tool resolves to canonical (e.g. \"bench\" → \"Bench Press\")."
        },
        "recent_limit": {
          "type": "integer",
          "description": "How many recent sessions containing this exercise to surface. Optional — default 10, capped at 50."
        },
        "since_date": {
          "type": "string",
          "description": "Optional YYYY-MM-DD lower bound for the Recent block. Does not affect the PR section, which is always all-time."
        }
      },
      "required": [
        "exercise_name"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_workout",
    "description": "Permanently delete a workout session and all its exercises and sets. Use when the user wants to remove a logged workout entirely.\n\nFIND THE SESSION: pass session_id if already known. Otherwise pass session_date (YYYY-MM-DD, defaults to today) and, only if more than one session was logged that day, name (a substring of the workout's focus/type, e.g. \"Push\" or \"Leg Day\", case-insensitive) to narrow it down. This action is irreversible and removes the session, all supersets, and all sets — a match that isn't exactly one session returns an error explaining why, with nothing deleted; retry with session_id or a narrower name, never guess.\n\nSAVED WORKOUTS: pass saved_workout_id to delete a reusable Saved Workout instead of completed workout history.",
    "annotations": {
      "title": "Delete a workout",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "integer",
          "description": "Session ID, if already known. Alternative to session_date + name — see FIND THE SESSION above."
        },
        "session_date": {
          "type": "string",
          "description": "Date the session was logged. Format: YYYY-MM-DD. Used with name to find the session when session_id is omitted; defaults to today if both are omitted."
        },
        "name": {
          "type": "string",
          "description": "Substring of the workout's focus/type (e.g. \"Push\", \"Leg Day\"), case-insensitive, to disambiguate multiple sessions on the same date. Only used when session_id is omitted."
        },
        "saved_workout_id": {
          "type": "integer",
          "description": "Saved Workout ID to delete. When present, deletes the reusable prescription and does not touch completed workout history."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_workout",
    "description": "UNIT INPUTS: never convert units yourself. Pass the user's number for a _lb / _mi / _in field exactly as stated, and when they gave kg / km / cm set input_weight_unit / input_distance_unit / input_length_unit in that same object. Omit the companion when the number is already lb / mi / in. The tool converts once before storage, so this overrides any wording that asks you to do the arithmetic.\n\nUpdate a workout session: correct metadata, fix set values, rename/add/remove exercises or individual sets, or move exercises between supersets. Use for any post-log correction.\n\nFIND THE SESSION: pass session_id if already known. Otherwise pass session_date (YYYY-MM-DD, defaults to today) and, only if more than one session was logged that day, name (a substring of the workout's focus/type, case-insensitive) to narrow it down. A match that isn't exactly one session returns an error explaining why, with nothing changed — retry with session_id or a narrower name, never guess. get_workout still gives full detail (exercise names, slot names SS1/SS2/WarmUp/Finisher) when needed; list_exercises first if adding or renaming. Call with only the fields that change — operations can combine in one call.\n\nOPERATIONS:\n- Metadata: date, focus_type, location, notes, rpe, heart_points_moderate/peak.\n- set_updates: patch reps/weight/notes/equipment on a set, addressed by set_id OR by exercise_name + set_position (1-based, matches get_workout's \"Set N\").\n- remove_sets: delete sets, same set_id-or-exercise_name+set_position addressing; remaining sets renumber; an emptied exercise/slot is removed automatically.\n- rename_exercises: renames every set of an exercise in place (preserves set IDs, RPE, notes; rebuilds NSI), never remove + add.\n- remove_exercises: deletes all sets for named exercises; empty slots removed automatically.\n- add_exercises: new exercises with sets; to_superset_slot joins an existing slot, omit for standalone.\n- move_exercises: reassigns an exercise to a different slot; \"new\" makes it standalone.\n\nSUPERSET SLOTS: rename_exercises/remove_exercises match by exercise name alone unless scoped. If a name is in more than one slot and the user means only one, pass superset_slot (or { name, superset_slot } for remove_exercises) naming that slot from get_workout. Omitting it hits every occurrence, a real corruption risk.\n\nLITERAL NAME: literal_name: true keeps the user's exact wording instead of the closest library match, skips the resolver, and gets no NSI score (no benchmark to compare an unmatched name against). Use for \"call it exactly X\", \"not the standard one\", \"literally X\", or a rejected match. Applies below.\n\nEQUIPMENT (load basis): dumbbell_pair is one dumbbell in EACH hand, weight_lb PER HAND (2x for NSI); dumbbell_single is one implement total. Laterality (single-leg/arm) does NOT decide this alone. Set it when the user describes the load (each hand, machine, band); a wrong or missing tag silently halves or doubles NSI. Values: barbell, dumbbell_pair, dumbbell_single, machine, kettlebell, bodyweight, band, cable, trx, other.\n\nA set_id or exercise_name+set_position matching more than one set (the same exercise in two superset slots) is ambiguous and errors rather than guessing — use the exact set_id from get_workout to disambiguate.\n\nThe result discloses a mismatched name from rename_exercises/add_exercises; relay it in your own words. If a name matches nothing closely enough, the result names near-miss library exercises; ask the user which they meant rather than accept the unscored custom log silently.\n\nINFER — do not ask: session_date defaults to today, set positions count from 1 per exercise. Slot names and set_ids beyond what's inferable come from get_workout; canonical exercise names come from list_exercises.\n\nSAVED WORKOUTS: pass saved_workout_id to edit a reusable Saved Workout instead of completed workout history. Use saved_workout_title and/or saved_exercise_updates. For progression requests, inspect real exercise history first rather than applying a deterministic formula.",
    "annotations": {
      "title": "Update a workout",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "integer",
          "description": "Session ID, if known. See FIND THE SESSION above."
        },
        "session_date": {
          "type": "string",
          "description": "YYYY-MM-DD, defaults to today. Finds the session when session_id is omitted. Distinct from `date`, which CHANGES the stored date."
        },
        "name": {
          "type": "string",
          "description": "Substring of the focus/type, e.g. \"Push\", case-insensitive, to pick between sessions on session_date. Only without session_id."
        },
        "date": {
          "type": "string",
          "description": "New session date, YYYY-MM-DD."
        },
        "focus_type": {
          "type": "string",
          "description": "New category, e.g. Push, Pull, Legs."
        },
        "location": {
          "type": "string",
          "description": "New location, e.g. Gym, Home."
        },
        "notes": {
          "type": "string",
          "description": "New session notes."
        },
        "rpe": {
          "type": "number",
          "description": "Session RPE, 1-10, half steps allowed: 5 moderate, 7 hard, 9 one rep left, 10 failure. Infer from comments about overall difficulty, or omit."
        },
        "heart_points_moderate": {
          "type": "integer",
          "description": "New moderate heart points."
        },
        "heart_points_peak": {
          "type": "integer",
          "description": "New peak heart points."
        },
        "set_updates": {
          "type": "array",
          "description": "Individual set corrections.",
          "items": {
            "type": "object",
            "properties": {
              "set_id": {
                "type": "integer",
                "description": "set_id:N from get_workout. Alternative to exercise_name + set_position."
              },
              "exercise_name": {
                "type": "string",
                "description": "Exact name from get_workout, with set_position instead of set_id."
              },
              "set_position": {
                "type": "integer",
                "description": "1-based set number, get_workout's \"Set N\". Needs exercise_name."
              },
              "reps": {
                "type": "integer",
                "description": "Corrected rep count."
              },
              "weight_lb": {
                "type": "number",
                "description": "Corrected load. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
              },
              "is_band": {
                "type": "boolean",
                "description": "True for a band."
              },
              "band_label_lb": {
                "type": "number",
                "description": "Band label weight; defaults to weight_lb. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
              },
              "hold_length_sec": {
                "type": "integer",
                "description": "Corrected hold/carry seconds."
              },
              "rpe": {
                "type": "number",
                "description": "RPE for this one set, same 1-10 scale as the session rpe field. Only when the user gives per-set effort."
              },
              "set_notes": {
                "type": "string",
                "description": "Updated per-set note."
              },
              "equipment": {
                "type": "string",
                "enum": [
                  "barbell",
                  "dumbbell_pair",
                  "dumbbell_single",
                  "machine",
                  "kettlebell",
                  "bodyweight",
                  "band",
                  "cable",
                  "trx",
                  "other"
                ],
                "description": "Corrects the load basis on this ONE set in place. See EQUIPMENT above. Optional."
              },
              "input_weight_unit": {
                "type": "string",
                "enum": [
                  "lb",
                  "kg"
                ],
                "description": "Set to kg when the user gave kg for the _lb fields in this object. Omit when they are already lb."
              }
            }
          }
        },
        "remove_sets": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "set_id": {
                "type": "integer",
                "description": "set_id:N from get_workout. Alternative to exercise_name + set_position."
              },
              "exercise_name": {
                "type": "string",
                "description": "Exact name from get_workout, with set_position instead of set_id."
              },
              "set_position": {
                "type": "integer",
                "description": "1-based set number. Needs exercise_name."
              }
            }
          },
          "description": "Sets to delete. See OPERATIONS above."
        },
        "rename_exercises": {
          "type": "array",
          "description": "Rename logged exercises in place.",
          "items": {
            "type": "object",
            "properties": {
              "from_name": {
                "type": "string",
                "description": "Current name from get_workout, exact."
              },
              "to_name": {
                "type": "string",
                "description": "New name. Canonical name from list_exercises where one matches."
              },
              "literal_name": {
                "type": "boolean",
                "description": "Keep exactly as given, skipping the library match. See LITERAL NAME above. Default false. Applies to to_name."
              },
              "superset_slot": {
                "type": "string",
                "description": "Scope to ONE slot (e.g. \"SS1\"). See SUPERSET SLOTS above; required when the name is in multiple slots."
              }
            },
            "required": [
              "from_name",
              "to_name"
            ]
          }
        },
        "remove_exercises": {
          "type": "array",
          "description": "Exercises to remove; matched sets deleted, empty slots removed automatically. See SUPERSET SLOTS above.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Exact name from get_workout."
              },
              "superset_slot": {
                "type": "string",
                "description": "Scope to ONE slot (e.g. \"SS1\"). See SUPERSET SLOTS above; required when the name is in multiple slots."
              }
            },
            "required": [
              "name"
            ]
          }
        },
        "add_exercises": {
          "type": "array",
          "description": "New exercises to add. Call list_exercises first for canonical names.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Canonical name from list_exercises."
              },
              "literal_name": {
                "type": "boolean",
                "description": "Keep exactly as given, skipping the library match. See LITERAL NAME above. Default false."
              },
              "to_superset_slot": {
                "type": "string",
                "description": "Existing slot to join, e.g. \"SS1\". Omit for a new standalone slot."
              },
              "slot_type": {
                "type": "string",
                "enum": [
                  "warmup",
                  "working",
                  "finisher"
                ],
                "description": "Slot type for a new slot, default working. Ignored with to_superset_slot."
              },
              "equipment": {
                "type": "string",
                "enum": [
                  "barbell",
                  "dumbbell_pair",
                  "dumbbell_single",
                  "machine",
                  "kettlebell",
                  "bodyweight",
                  "band",
                  "cable",
                  "trx",
                  "other"
                ],
                "description": "Load basis for every set of this exercise. See EQUIPMENT above. Optional; overrides the auto guess."
              },
              "sets": {
                "type": "array",
                "description": "Every set for this exercise.",
                "items": {
                  "type": "object",
                  "properties": {
                    "reps": {
                      "type": "integer",
                      "description": "Reps this set."
                    },
                    "weight_lb": {
                      "type": "number",
                      "description": "Load. Omit for bodyweight. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
                    },
                    "is_bodyweight": {
                      "type": "boolean",
                      "description": "True if no external weight."
                    },
                    "is_band": {
                      "type": "boolean",
                      "description": "True for a band."
                    },
                    "band_label_lb": {
                      "type": "number",
                      "description": "Band label weight; defaults to weight_lb. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
                    },
                    "hold_length_sec": {
                      "type": "integer",
                      "description": "Hold/carry seconds."
                    },
                    "rpe": {
                      "type": "number",
                      "description": "RPE for this one set, same 1-10 scale as the session rpe field. Only when the user gives per-set effort."
                    },
                    "notes": {
                      "type": "string",
                      "description": "Per-set notes."
                    },
                    "input_weight_unit": {
                      "type": "string",
                      "enum": [
                        "lb",
                        "kg"
                      ],
                      "description": "Set to kg when the user gave kg for the _lb fields in this object. Omit when they are already lb."
                    }
                  }
                }
              }
            },
            "required": [
              "name",
              "sets"
            ]
          }
        },
        "move_exercises": {
          "type": "array",
          "description": "Move exercises between superset slots.",
          "items": {
            "type": "object",
            "properties": {
              "exercise_name": {
                "type": "string",
                "description": "Exact name from get_workout."
              },
              "to_superset_slot": {
                "type": "string",
                "description": "Target slot, e.g. \"SS1\", or \"new\" for standalone."
              }
            },
            "required": [
              "exercise_name",
              "to_superset_slot"
            ]
          }
        },
        "saved_workout_id": {
          "type": "integer",
          "description": "Saved Workout ID to update instead of a completed workout session."
        },
        "saved_workout_title": {
          "type": "string",
          "description": "Optional new title for the Saved Workout."
        },
        "saved_exercise_updates": {
          "type": "array",
          "description": "Optional prescription edits matched by exercise name inside the Saved Workout.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Exercise name in the Saved Workout. Required."
              },
              "sets": {
                "type": "integer",
                "description": "Updated prescribed set count. Optional."
              },
              "reps": {
                "type": "integer",
                "description": "Updated prescribed reps per set. Optional."
              },
              "weight_lb": {
                "type": "number",
                "description": "Updated prescribed load in pounds. Optional. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
              },
              "input_weight_unit": {
                "type": "string",
                "enum": [
                  "lb",
                  "kg"
                ],
                "description": "Set to kg when the user gave kg for the _lb fields in this object. Omit when they are already lb."
              }
            },
            "required": [
              "name"
            ]
          }
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_wearable",
    "description": "Log daily wearable/manual health metrics (RHR, HRV, Zone Minutes / AZM, VO2max, calories eaten / dietary energy, stress, supplemental steps, and physiological vitals including SpO₂, respiratory rate, skin temperature, blood pressure, blood glucose, and core temperature).\n\nVITALS — use the vital fields for manual/home/device readings and corrections, including a finger-stick, CGM, home glucose meter, or wearable/Apple Health/Health Connect value the user explicitly wants stored manually. A glucose value from an actual lab report or blood draw belongs in log_lab_results instead, not here.\n\nSTEPS — read before using step_count: manual step_count is ADDITIVE — it adds on top of whatever a connected wearable (Fitbit, Oura, Apple Health, Health Connect) already recorded that day; it never replaces or overrides device data. Only use it when the user explicitly says they walked steps their device did NOT capture (phone left home, battery died, device not worn). If the user says sync is wrong, steps look doubled, or they want to fix/override/replace device data: do NOT pass step_count — explain that manual steps add on top, and sync issues need investigating at the device level.\n\nALL OTHER FIELDS (RHR, HRV, AZM, VO2max, stress, and physiological vitals) replace the existing manual value for that day and are safe to use for corrections.\n\nINFER — do not ask: date defaults to today unless the user says otherwise.\n\nIDEMPOTENT: if the values you'd log already exist for that date (any source), the tool returns a no-op success — report this as \"already had data\", not \"failed\".",
    "annotations": {
      "title": "Log wearable data",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "Date for the entry. Format: YYYY-MM-DD. Default to today."
        },
        "step_count": {
          "type": "integer",
          "description": "Supplemental steps NOT captured by any connected wearable. ADDITIVE — adds to device data, never replaces it. Only use when user explicitly says their device missed these steps."
        },
        "resting_hr": {
          "type": "integer",
          "description": "Resting heart rate in BPM. Optional."
        },
        "hrv_morning": {
          "type": "number",
          "description": "Morning HRV in milliseconds (RMSSD or similar). Optional."
        },
        "active_zone_min_moderate": {
          "type": "integer",
          "description": "Zone Minutes (also called Active Zone Minutes by Fitbit, Heart Points by Google Fit) at moderate intensity. Optional."
        },
        "active_zone_min_peak": {
          "type": "integer",
          "description": "Zone Minutes (also called Active Zone Minutes by Fitbit, Heart Points by Google Fit) at peak intensity. Optional."
        },
        "vo2max_estimate": {
          "type": "number",
          "description": "VO2max estimate (ml/kg/min). Optional."
        },
        "dietary_calories": {
          "type": "integer",
          "description": "Dietary energy / calories eaten for the day in kcal, imported from a connected health app (Apple Health / Health Connect). Optional."
        },
        "stress_score": {
          "type": "integer",
          "description": "Stress score (0–100 scale). Optional."
        },
        "spo2_avg_pct": {
          "type": "number",
          "description": "SpO₂ in %. Manual/home/device reading. Optional."
        },
        "respiratory_rate_bpm": {
          "type": "number",
          "description": "Breathing in br/min. Manual/home/device reading. Optional."
        },
        "skin_temp_c": {
          "type": "number",
          "description": "Skin temperature in °C. Manual/home/device reading. Optional."
        },
        "systolic_bp_mm_hg": {
          "type": "number",
          "description": "Systolic blood pressure in mmHg. Provide with diastolic_bp_mm_hg. Manual/home/device reading. Optional."
        },
        "diastolic_bp_mm_hg": {
          "type": "number",
          "description": "Diastolic blood pressure in mmHg. Provide with systolic_bp_mm_hg. Manual/home/device reading. Optional."
        },
        "blood_glucose_mg_dl": {
          "type": "number",
          "description": "Blood glucose in mg/dL. Manual/home/device reading. Optional."
        },
        "core_body_temp_c": {
          "type": "number",
          "description": "Core temperature in °C. Manual/home/device reading. Optional."
        },
        "confirm_supplemental": {
          "type": "boolean",
          "description": "Set to true ONLY after the user has explicitly confirmed these steps were walked without their device and were not captured. Do not set true based on your own inference."
        }
      },
      "required": [
        "date"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_wearable_data",
    "description": "List daily wearable data (steps, RHR, HRV, Zone Minutes / AZM including the vigorous-intensity breakdown, VO2max, calories eaten / dietary energy, calories burned / active energy / total energy expenditure, maintenance calories / TDEE, stress, and physiological vital signs reported by connected sources or manual overrides) within a date range. Use when the user asks about their step count, heart rate, HRV trends, vigorous minutes, calories eaten / dietary energy, calories burned, active or total energy expenditure, maintenance calories, TDEE, cardio fitness, blood glucose, vital signs, or any wearable metrics over time.\n\nZone Minutes (a.k.a. Active Zone Minutes) are shown as a daily total plus, when the per-zone breakdown is available, a moderate (1 pt/min) vs vigorous (2 pts/min — Cardio + Peak zones) split. The zone boundaries are personalized to the user's own resting and maximum heart rate, so they are not a fixed BPM.\n\nCalories burned per day shows active + resting where both are known; when a device reports active energy with no resting figure, a resting estimate is derived from the user's profile BMR and labelled an estimate, never shown as measured. A trailing Maintenance (TDEE) line reports current maintenance calories and which method produced it (formula estimate vs. logged weight trend), or names the missing profile fields when TDEE can't be computed.\n\nMaximum range: 31 days per call. For longer periods, make multiple calls with sequential date ranges.\n\nINFER — do not ask:\n- start_date: default to 14 days ago\n- end_date: default to today",
    "annotations": {
      "title": "List wearable data",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Default: 14 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_body_metrics",
    "description": "UNIT INPUTS: never convert units yourself. Pass the user's number for a _lb / _mi / _in field exactly as stated, and when they gave kg / km / cm set input_weight_unit / input_distance_unit / input_length_unit in that same object. Omit the companion when the number is already lb / mi / in. The tool converts once before storage, so this overrides any wording that asks you to do the arithmetic.\n\nLog or update body composition metrics for a given date. Use when the user shares weight, body fat percentage, or any other body composition reading — whether typed manually, copy-pasted from a smart scale app, or described from a photo of a scale display.\n\nPROACTIVE DATA COLLECTION: If the user hasn't shared their data yet, ask them to copy-paste the output from their scale app or upload a photo of the display — this lets you parse all fields at once instead of asking one by one.\n\nINFER — do not ask:\n- date: default to today; infer from context (\"this morning\", \"yesterday\")\n- derived fields (lean_mass_lb, fat_mass_lb): calculate from weight and body fat % if possible — lean = weight × (1 - bf%/100), fat = weight × bf%/100\n\nThe *_pct fields are percentages 0-100. The *_in fields are manual tape measurements, not bioimpedance scale readings.\n\nBMI is not a field here. It is derived automatically at read time from the user's canonical height and their resolved weight for the day -- never ask the user for BMI, and never try to log it.\n\nEvery field except date is optional; log any subset. One row per day. Calling this tool twice on the same date updates the existing entry (upsert).",
    "annotations": {
      "title": "Log body metrics",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "Date of the measurement. Format: YYYY-MM-DD. Default to today."
        },
        "weight_lb": {
          "type": "number",
          "description": "Body weight. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
        },
        "body_fat_pct": {
          "type": "number",
          "description": "Body fat percentage (0-100)."
        },
        "lean_mass_lb": {
          "type": "number",
          "description": "Lean (non-fat) mass. Calculate from weight and body fat % if not explicitly stated. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
        },
        "fat_mass_lb": {
          "type": "number",
          "description": "Fat mass. Calculate from weight and body fat % if not explicitly stated. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
        },
        "visceral_fat_rating": {
          "type": "number",
          "description": "Visceral fat rating (scale varies by device, typically 1-59)."
        },
        "hydration_pct": {
          "type": "number",
          "description": "Body water/hydration percentage."
        },
        "bone_mass_lb": {
          "type": "number",
          "description": "Bone mass. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
        },
        "muscle_mass_lb": {
          "type": "number",
          "description": "Skeletal muscle mass. Skeletal muscle tissue specifically, NOT lean_mass_lb, which is total non-fat mass including water, organs and bone. In lb, or kg with input_weight_unit set. See UNIT INPUTS."
        },
        "skeletal_muscle_pct": {
          "type": "number",
          "description": "Skeletal muscle percentage (0-100)."
        },
        "protein_pct": {
          "type": "number",
          "description": "Protein percentage (0-100)."
        },
        "waist_in": {
          "type": "number",
          "description": "Waist circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "hips_in": {
          "type": "number",
          "description": "Hip circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "chest_in": {
          "type": "number",
          "description": "Chest circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "bicep_l_in": {
          "type": "number",
          "description": "Left bicep circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "bicep_r_in": {
          "type": "number",
          "description": "Right bicep circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "thigh_l_in": {
          "type": "number",
          "description": "Left thigh circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "thigh_r_in": {
          "type": "number",
          "description": "Right thigh circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "calf_l_in": {
          "type": "number",
          "description": "Left calf circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "calf_r_in": {
          "type": "number",
          "description": "Right calf circumference. In in, or cm with input_length_unit set. See UNIT INPUTS."
        },
        "notes": {
          "type": "string",
          "description": "Any context worth noting (e.g. \"post-workout\", \"morning fasted\")."
        },
        "input_weight_unit": {
          "type": "string",
          "enum": [
            "lb",
            "kg"
          ],
          "description": "Set to kg when the user gave kg for the _lb fields in this object. Omit when they are already lb."
        },
        "input_length_unit": {
          "type": "string",
          "enum": [
            "in",
            "cm"
          ],
          "description": "Set to cm when the user gave cm for the _in fields in this object. Omit when they are already in."
        }
      },
      "required": [
        "date"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_body_metrics",
    "description": "List body composition entries within a date range. Use when the user asks about their weight history, body fat trend, or any body metrics over time.\n\nMaximum range: 31 days per call. For longer periods, make multiple calls with sequential date ranges.\n\nINFER — do not ask:\n- start_date: default to 30 days ago\n- end_date: default to today\n\nBMI in the output is derived from the user's canonical height and that day's resolved weight -- do not recompute it yourself.",
    "annotations": {
      "title": "List body metrics",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Default: 30 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_sleep",
    "description": "Log a sleep entry. Use when the user shares sleep data — total duration, score, stage breakdown, bedtime, or wake time — from Fitbit, Oura, Whoop, Apple Health, or manual recall.\n\nPROACTIVE DATA COLLECTION: If the user says they want to log sleep but hasn't shared numbers, ask: \"How many hours did you sleep, and do you have a sleep score or stage breakdown from your tracker?\" They can paste or describe the summary screen.\n\nINFER — do not ask:\n- date: date the primary sleep session ended / wake date (night ending on this date); default to today\n\nYou may log any subset of fields. One row per day. Calling this tool twice on the same date updates the existing entry (upsert). Entries made through this tool are always tagged as manual — the wearable-provider sources (Fitbit/Oura/Apple Health) are reserved for the actual auto-sync pipelines.",
    "annotations": {
      "title": "Log sleep",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "Date of the sleep entry (night ending on this date). Format: YYYY-MM-DD. Default to today."
        },
        "total_hours": {
          "type": "number",
          "description": "Total sleep duration in hours (e.g. 7.5). Optional."
        },
        "sleep_score": {
          "type": "integer",
          "description": "Sleep quality score on a 0-100 scale (matches wearable scoring). For a 1-10 self-rating, multiply by 10 first. Optional."
        },
        "deep_sleep_hours": {
          "type": "number",
          "description": "Deep/slow-wave sleep in hours. Optional — include if the tracker reports it."
        },
        "rem_sleep_hours": {
          "type": "number",
          "description": "REM sleep in hours. Optional — include if the tracker reports it."
        },
        "light_sleep_hours": {
          "type": "number",
          "description": "Light sleep in hours. Optional — include if the tracker reports it."
        },
        "awakenings": {
          "type": "integer",
          "description": "Number of times woken during the night. Optional."
        },
        "bedtime": {
          "type": "string",
          "description": "Bedtime / primary sleep session start time. Format: ISO 8601 timestamp (e.g. 2026-08-16T22:47:00-04:00) or HH:MM wall-clock time. Optional."
        },
        "wake_time": {
          "type": "string",
          "description": "Wake time / primary sleep session end time. Format: ISO 8601 timestamp (e.g. 2026-08-17T06:21:00-04:00) or HH:MM wall-clock time. Optional."
        }
      },
      "required": [
        "date"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_sleep",
    "description": "List sleep log entries within a date range. Each entry includes total duration, sleep score, stage breakdown, and the canonical bedtime and wake_time (full ISO 8601 timestamps with preserved timezone offset) for the user's primary overnight sleep session (excluding daytime naps).\n\nMaximum range: 31 days per call. For longer periods, make multiple calls with sequential date ranges.\n\nINFER — do not ask:\n- start_date: default to 14 days ago\n- end_date: default to today",
    "annotations": {
      "title": "List sleep",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Default: 14 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_lab_markers",
    "description": "Returns all LOINC-coded markers in the reference library: canonical name, LOINC code, panel, typical unit, and common aliases. Call this BEFORE log_lab_results to match user-provided marker names to canonical entries — same pattern as list_exercises for workouts. Prevents name drift and ensures trending works across lab visits.",
    "annotations": {
      "title": "List lab markers",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_lab_results",
    "description": "Log one or more blood test or biomarker results. Use when the user shares lab values — copy-pasted from a Quest/LabCorp PDF, typed from a paper report, or described from a photo of their results.\n\nGLUCOSE ROUTING: use this tool for glucose only when it is an actual lab/blood-draw result or part of a reported lab panel. Finger-stick, CGM, home meter, wearable, Apple Health, or Health Connect glucose — including a manual correction to daily glucose — belongs in log_wearable as blood_glucose_mg_dl, not here.\n\nREQUIRED WORKFLOW: 1) call list_lab_markers for canonical names and LOINC codes. 2) for each marker the user provides, find the best match and use its canonical marker_name and loinc_code. 3) if no match exists, use the name as stated and omit loinc_code.\n\nIf the user says they have lab results but hasn't shared them, prompt: \"You can paste the text from your lab report PDF, or upload a photo of the results page — I'll parse all the values at once.\"\n\nINFER — do not ask: date (look for a collection/drawn date in the pasted text, default today), panel_name (from list_lab_markers for matched markers, infer for unmatched), flag (extract from the report if present: \"H\", \"L\", \"HH\", \"LL\", \"A\"), ref_range_low/high (parse from the report if shown), lab_name (from the report header, same for all markers in a visit).\n\nSubmit all markers from a single lab visit in one call.",
    "annotations": {
      "title": "Log lab results",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "results": {
          "type": "array",
          "description": "Array of individual lab marker results. Required — submit all markers from the visit in a single call.",
          "items": {
            "type": "object",
            "properties": {
              "date": {
                "type": "string",
                "description": "Collection/draw date. Format: YYYY-MM-DD. Required."
              },
              "panel_name": {
                "type": "string",
                "description": "Panel name from list_lab_markers, or inferred. Required."
              },
              "marker_name": {
                "type": "string",
                "description": "Canonical name from list_lab_markers if matched; otherwise name as stated. Required."
              },
              "loinc_code": {
                "type": "string",
                "description": "LOINC code from list_lab_markers. Include for all matched markers."
              },
              "result_value": {
                "type": "number",
                "description": "Numeric result value. Required."
              },
              "result_unit": {
                "type": "string",
                "description": "Unit of measurement (e.g. \"mg/dL\"). Optional but include if present."
              },
              "flag": {
                "type": "string",
                "description": "Lab flag: \"H\", \"L\", \"HH\", \"LL\", or \"A\". Optional."
              },
              "ref_range_low": {
                "type": "number",
                "description": "Lab-reported reference range lower bound. Optional."
              },
              "ref_range_high": {
                "type": "number",
                "description": "Lab-reported reference range upper bound. Optional."
              },
              "lab_name": {
                "type": "string",
                "description": "Lab name (e.g. \"Quest Diagnostics\", \"LabCorp\"). Optional."
              },
              "notes": {
                "type": "string",
                "description": "Additional notes. Optional."
              }
            },
            "required": [
              "date",
              "panel_name",
              "marker_name",
              "result_value"
            ]
          }
        }
      },
      "required": [
        "results"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_lab_results",
    "description": "List lab/biomarker results within a date range, including each result's ID. update_lab_result and delete_lab_result can resolve a result on their own from date (+ optional marker or panel_name), so this is no longer required before either — use it to review lab history, answer questions about blood work trends or specific marker values over time, or get an id after an ambiguous update_lab_result/delete_lab_result match. Optionally filter by panel or marker name.\n\nINFER — do not ask:\n- start_date: default to 365 days ago (labs are infrequent)\n- end_date: default to today",
    "annotations": {
      "title": "List lab results",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Default: 365 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Default: today."
        },
        "panel_name": {
          "type": "string",
          "description": "Optional — filter to a specific panel (e.g. \"Lipid Panel\")."
        },
        "marker_name": {
          "type": "string",
          "description": "Optional — filter to a specific marker (e.g. \"LDL Cholesterol\")."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_lab_result",
    "description": "Update one or more fields on an existing lab result. Use when the user wants to correct a result already logged, most often its collection date. Only send the fields that need to change; omit all others.\n\nSELECTOR, pass exactly one of id, date, or date+marker:\n- id: addresses one marker's row. Any editable field may change.\n- date (optionally narrowed by panel_name): addresses every result from that draw at once. Only new_date, panel_name_new, and lab_name may change this way. result_value, result_unit, marker_name, flag, and notes are per-marker fields and require a single row, so one call can never overwrite every marker in a draw with the same value.\n- date + marker (a marker-name substring, case-insensitive, optionally narrowed by panel_name): resolves to one marker's row, same as id. Errors with candidate IDs if more than one marker on that date matches.",
    "annotations": {
      "title": "Update a lab result",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Lab result ID. Selects a single marker row. Alternative to date."
        },
        "date": {
          "type": "string",
          "description": "Collection date of the draw to update. Format: YYYY-MM-DD. Selects every result from that draw, or (with marker) one row. Alternative to id."
        },
        "marker": {
          "type": "string",
          "description": "Optional with date: marker-name substring, case-insensitive (e.g. \"LDL\"), narrowing the date selector to a single marker row so per-marker fields can be edited without an id. Ignored when id is given."
        },
        "panel_name": {
          "type": "string",
          "description": "Optional, narrows a date (or date+marker) selector to one panel within that draw (e.g. \"Lipid Panel\"). Ignored when id is given."
        },
        "new_date": {
          "type": "string",
          "description": "New collection date. Format: YYYY-MM-DD. Optional, omit if not changing. This is the main reason to call this tool, and it works with any selector."
        },
        "result_value": {
          "type": "number",
          "description": "New numeric result value. Optional, omit if not changing. Only allowed when selecting by id or date+marker."
        },
        "result_unit": {
          "type": "string",
          "description": "New unit of measurement (e.g. \"mg/dL\"). Optional, omit if not changing. Only allowed when selecting by id or date+marker."
        },
        "marker_name": {
          "type": "string",
          "description": "New marker name. Optional, omit if not changing. Only allowed when selecting by id or date+marker."
        },
        "panel_name_new": {
          "type": "string",
          "description": "New panel name. Optional, omit if not changing. Works with any selector."
        },
        "lab_name": {
          "type": "string",
          "description": "New lab name (e.g. \"Quest Diagnostics\", \"LabCorp\"). Optional, omit if not changing. Works with any selector."
        },
        "flag": {
          "type": "string",
          "description": "New lab flag: \"H\", \"L\", \"HH\", \"LL\", or \"A\". Optional, omit if not changing. Only allowed when selecting by id or date+marker."
        },
        "notes": {
          "type": "string",
          "description": "New notes. Optional, omit if not changing. Only allowed when selecting by id or date+marker."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_lab_result",
    "description": "Permanently delete one or more lab results. Use when the user explicitly asks to remove or delete a logged lab result. Never guess a selector.\n\nSELECTOR, pass exactly one of id, date, or date+marker:\n- id: deletes a single marker's row.\n- date (optionally narrowed by panel_name): deletes every result from that draw at once. This is irreversible and can remove many rows in one call. Confirm with the user before a date-scoped delete, especially one not narrowed by panel_name or marker.\n- date + marker (a marker-name substring, case-insensitive, optionally narrowed by panel_name): resolves to and deletes one marker's row, same as id. Errors with candidate IDs if more than one marker on that date matches.",
    "annotations": {
      "title": "Delete a lab result",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Lab result ID. Deletes a single marker row. Alternative to date."
        },
        "date": {
          "type": "string",
          "description": "Collection date of the draw to delete. Format: YYYY-MM-DD. Deletes every result from that draw, or (with marker) one row. Alternative to id."
        },
        "marker": {
          "type": "string",
          "description": "Optional with date: marker-name substring, case-insensitive (e.g. \"LDL\"), narrowing the date selector to delete a single marker row instead of the whole draw. Ignored when id is given."
        },
        "panel_name": {
          "type": "string",
          "description": "Optional, narrows a date (or date+marker) selector to one panel within that draw (e.g. \"Lipid Panel\"). Ignored when id is given."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_injury",
    "description": "Log a new injury or aggravation. Use when the user mentions getting hurt, feeling pain, straining something, or describes an injury. Injuries are date ranges — they start on a date and are ongoing until an end_date is set.\n\nINFER — do not ask:\n- start_date: default to today\n- severity: estimate from description (minor twinge=2-3, moderate pain=5-6, severe/acute=8-9)\n- status: default to 'Active' for new injuries\n- affected_movements: infer from body part and injury type (e.g. shoulder strain → pressing, overhead)\n- side: infer from description if mentioned (e.g. \"right shoulder\" → Right)\n\nASK only if body_part is entirely unclear.",
    "annotations": {
      "title": "Log an injury",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Date injury started. Format: YYYY-MM-DD. Default: today."
        },
        "end_date": {
          "type": "string",
          "description": "Date injury resolved. Format: YYYY-MM-DD. Default: null (ongoing). Set when injury is fully resolved."
        },
        "body_part": {
          "type": "string",
          "description": "Body part affected (e.g. 'Shoulder', 'Lower Back', 'Knee'). Required."
        },
        "side": {
          "type": "string",
          "enum": [
            "Left",
            "Right",
            "Both",
            "Center"
          ],
          "description": "Side of body. Optional — infer from description."
        },
        "injury_type": {
          "type": "string",
          "description": "Type of injury (e.g. 'Strain', 'Tendonitis', 'Sprain', 'Disc', 'Soreness', 'Acute'). Required — infer from description."
        },
        "severity": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10,
          "description": "Severity 1-10 (1=minor, 10=severe). Required — estimate from description."
        },
        "status": {
          "type": "string",
          "enum": [
            "Active",
            "Monitoring",
            "Resolved"
          ],
          "description": "Status. Default: 'Active'. Auto-set to 'Resolved' if end_date is provided."
        },
        "affected_movements": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Movements affected (e.g. ['Pressing', 'Overhead', 'Bench Press']). Infer from body part."
        },
        "notes": {
          "type": "string",
          "description": "Additional context about how injury occurred, symptoms, etc. Optional."
        }
      },
      "required": [
        "body_part",
        "injury_type",
        "severity"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_injuries",
    "description": "List injuries from the injury log. update_injury and delete_injury can resolve an injury on their own from injury (+ optional date), so this is no longer required before either — use it to review the injury log, answer questions about injury history or rehab progress, or get an id after an ambiguous update_injury/delete_injury match. Defaults to active and monitoring injuries.\n\nINFER — do not ask:\n- status: default to showing Active and Monitoring; use 'all' to include Resolved; use 'Resolved' for history only.",
    "annotations": {
      "title": "List injuries",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "enum": [
            "Active",
            "Monitoring",
            "Resolved",
            "all"
          ],
          "description": "Filter by status. Default: Active + Monitoring."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_injury",
    "description": "Update an existing injury entry. Use when the user reports an injury is improving, worsening, resolved, or wants to change details. When severity changes, the new value is automatically tracked in the severity history for trend analysis. Only send fields that need to change.\nSetting end_date automatically marks the injury as Resolved.\nUse severity_date to backfill historical severity changes (e.g., \"it was a 7 in January, dropped to 4 by March\").\n\nSELECTOR — pass id if known, or injury (a body part or injury type substring, case-insensitive, e.g. \"shoulder\") optionally narrowed by date (an injury active on that day). Exactly one of id or injury required. If injury matches more than one entry, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Update an injury",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Injury ID. Alternative to injury."
        },
        "injury": {
          "type": "string",
          "description": "Alternative to id: body part or injury type substring, case-insensitive (e.g. \"shoulder\"). Optionally narrow with date."
        },
        "date": {
          "type": "string",
          "description": "Optional, narrows the injury selector to one active on this date (YYYY-MM-DD). Ignored when id is given."
        },
        "start_date": {
          "type": "string",
          "description": "Updated start date. Format: YYYY-MM-DD. Optional."
        },
        "severity": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10,
          "description": "Updated severity 1-10. Optional. Change is tracked in severity history."
        },
        "severity_date": {
          "type": "string",
          "description": "Date for the severity entry in the history log. Format: YYYY-MM-DD. Default: today. Use to backfill past severity changes."
        },
        "status": {
          "type": "string",
          "enum": [
            "Active",
            "Monitoring",
            "Resolved"
          ],
          "description": "Updated status. Optional."
        },
        "side": {
          "type": "string",
          "enum": [
            "Left",
            "Right",
            "Both",
            "Center"
          ],
          "description": "Updated side. Optional."
        },
        "affected_movements": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Updated list of affected movements. Optional."
        },
        "end_date": {
          "type": "string",
          "description": "Date injury resolved. Format: YYYY-MM-DD. Auto-sets status to Resolved."
        },
        "notes": {
          "type": "string",
          "description": "Updated notes (replaces existing). Optional."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_injury",
    "description": "Permanently delete an injury entry. Also removes all severity history for that injury.\n\nSELECTOR — pass id if known, or injury (a body part or injury type substring, case-insensitive, e.g. \"shoulder\") optionally narrowed by date (an injury active on that day). Exactly one of id or injury required. If injury matches more than one entry, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Delete an injury",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Injury ID to delete. Alternative to injury."
        },
        "injury": {
          "type": "string",
          "description": "Alternative to id: body part or injury type substring, case-insensitive (e.g. \"shoulder\"). Optionally narrow with date."
        },
        "date": {
          "type": "string",
          "description": "Optional, narrows the injury selector to one active on this date (YYYY-MM-DD). Ignored when id is given."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_wellbeing",
    "description": "Log subjective wellbeing ratings for a day, week, month, or custom date range. Use when the user describes how they feel — energy level, mood, stress, or muscle soreness.\n\nSupports single-day entries (\"how I feel today\") and period entries (\"this week was stressful\", \"March was great\").\n\nIf an overlapping entry already exists for the requested period, returns a warning with the conflicting entry IDs — the user must update or delete existing entries first.\n\nINFER — do not ask:\n- period_start: default to today\n- period_end: default to same as period_start (single day). For \"this week\" use Monday–Sunday, for \"this month\" use first–last day.\n- ratings: estimate from description (\"exhausted\"=2, \"great energy\"=8, \"stressed out\"=8 stress, \"feeling good\"=7 mood)\n\nYou may log any subset of rating fields.",
    "annotations": {
      "title": "Log wellbeing",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "period_start": {
          "type": "string",
          "description": "Start date of period. Format: YYYY-MM-DD. Default: today."
        },
        "period_end": {
          "type": "string",
          "description": "End date of period. Format: YYYY-MM-DD. Default: same as period_start (single day). Use for week/month/custom ranges."
        },
        "energy": {
          "type": "integer",
          "description": "Energy level 1-10 (1=exhausted, 10=wired). Optional."
        },
        "mood": {
          "type": "integer",
          "description": "Mood 1-10 (1=terrible, 10=excellent). Optional."
        },
        "stress": {
          "type": "integer",
          "description": "Stress level 1-10 (1=calm, 10=overwhelmed). Optional."
        },
        "soreness": {
          "type": "integer",
          "description": "Muscle soreness 1-10 (1=none, 10=extreme DOMS). Optional."
        },
        "notes": {
          "type": "string",
          "description": "Free-text notes about how you feel. Optional."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_wellbeing",
    "description": "List wellbeing log entries within a date range. update_wellbeing and delete_wellbeing can resolve an entry on their own from date, so this is no longer required before either — use it to answer questions about mood, energy, stress, or soreness trends over time, or get an id after an ambiguous update_wellbeing/delete_wellbeing match.\n\nMaximum range: 31 days per call. For longer periods, make multiple calls with sequential date ranges.\n\nINFER — do not ask:\n- start_date: default to 14 days ago\n- end_date: default to today",
    "annotations": {
      "title": "List wellbeing",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Default: 14 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_wellbeing",
    "description": "Update an existing wellbeing entry. Only updates fields that are provided — omitted fields remain unchanged.\n\nSELECTOR — pass id if known, or date (any day within the entry's period) to resolve it. Exactly one of id or date required. If date matches more than one entry, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Update wellbeing",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Wellbeing entry ID. Alternative to date."
        },
        "date": {
          "type": "string",
          "description": "Alternative to id: a date (YYYY-MM-DD) that falls within the entry's period. Resolves only when exactly one entry matches."
        },
        "energy": {
          "type": "integer",
          "description": "Updated energy 1-10. Optional."
        },
        "mood": {
          "type": "integer",
          "description": "Updated mood 1-10. Optional."
        },
        "stress": {
          "type": "integer",
          "description": "Updated stress 1-10. Optional."
        },
        "soreness": {
          "type": "integer",
          "description": "Updated soreness 1-10. Optional."
        },
        "notes": {
          "type": "string",
          "description": "Updated notes (replaces existing). Optional."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_wellbeing",
    "description": "Permanently delete a wellbeing entry.\n\nSELECTOR — pass id if known, or date (any day within the entry's period) to resolve it. Exactly one of id or date required. If date matches more than one entry, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Delete a wellbeing entry",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Wellbeing entry ID to delete. Alternative to date."
        },
        "date": {
          "type": "string",
          "description": "Alternative to id: a date (YYYY-MM-DD) that falls within the entry's period. Resolves only when exactly one entry matches."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_supplements",
    "description": "List the user's medications and supplements. manage_supplement can resolve an item on its own from supplement_name, so this is no longer required before it — use it when the user asks what medications or supplements they're taking, asks to review their stack, or to get an id after an ambiguous manage_supplement match.\n\nINFER — do not ask:\n- filter: default to 'active' (current items); use 'all' if the user asks about history or a specific past period; use 'historical' for ended items only.\n- category: omit to return both medications and supplements; set to 'medication' or 'supplement' to filter by type.\n\nReturns each item's id, category, name, brand, dose, schedule, start_date, and end_date.",
    "annotations": {
      "title": "List supplements",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "filter": {
          "type": "string",
          "enum": [
            "active",
            "historical",
            "all"
          ],
          "description": "Which items to return. Default: 'active'."
        },
        "category": {
          "type": "string",
          "enum": [
            "medication",
            "supplement"
          ],
          "description": "Filter by category. Omit for both."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "manage_supplement",
    "description": "Add, update, end, or delete a medication or supplement. Use when the user describes their stack, adds a new item, changes a dose or schedule, says they stopped taking something, or wants to remove an entry.\n\nINFER — do not ask:\n- action: 'add' for a new item, 'update' for changing a field, 'end' when they stopped/finished a course, 'delete' only to remove the record entirely\n- category: 'medication' for prescription/OTC drugs and pharmaceuticals, 'supplement' for vitamins/minerals/herbs/other dietary supplements — default 'supplement' if unclear\n- start_date: today for new entries\n- end_date (for 'end'): today unless the user specifies otherwise\n\nASK the user only if name is missing for a new entry, or 'end' (set end_date) vs 'delete' (remove record) intent is ambiguous.\n\nSELECTOR for 'update', 'end', 'delete' — pass id if known, or supplement_name (case-insensitive substring, e.g. \"magnesium\") to resolve it. Exactly one of id or supplement_name required. If supplement_name matches more than one item, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Manage supplements",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "add",
            "update",
            "end",
            "delete"
          ],
          "description": "What to do. Required."
        },
        "id": {
          "type": "integer",
          "description": "ID. Required for update, end, delete unless supplement_name is given."
        },
        "supplement_name": {
          "type": "string",
          "description": "Alternative to id for update/end/delete: name substring, case-insensitive (e.g. \"magnesium\")."
        },
        "category": {
          "type": "string",
          "enum": [
            "medication",
            "supplement"
          ],
          "description": "Category: 'medication' for drugs/pharmaceuticals, 'supplement' for vitamins/minerals/herbs. Default: 'supplement'."
        },
        "name": {
          "type": "string",
          "description": "Name (e.g. 'Magnesium Glycinate' or 'Metformin'). Required for add; on update, sets a new name."
        },
        "brand": {
          "type": "string",
          "description": "Brand name. Optional."
        },
        "form": {
          "type": "string",
          "description": "Physical form: pill, capsule, tablet, softgel, powder, liquid, gummy, other. Optional."
        },
        "dose_amount": {
          "type": "number",
          "description": "Numeric dose quantity (e.g. 2 for '2 pills'). Required for add."
        },
        "dose_unit": {
          "type": "string",
          "description": "Unit for dose_amount: pills, capsules, tablets, softgels, g, mg, ml, IU, mcg, tbsp, scoop. Required for add."
        },
        "dose_per_unit": {
          "type": "number",
          "description": "Amount per individual unit (e.g. 240 for '240mg per pill'). Optional."
        },
        "unit_label": {
          "type": "string",
          "description": "Label for dose_per_unit (e.g. 'mg', 'IU'). Optional."
        },
        "frequency_type": {
          "type": "string",
          "enum": [
            "daily",
            "weekly",
            "monthly",
            "as_needed"
          ],
          "description": "Frequency category. Optional — default 'daily'."
        },
        "times_per_period": {
          "type": "integer",
          "description": "How many times per period (e.g. 2 for twice per week). Optional — default 1."
        },
        "period_unit": {
          "type": "string",
          "enum": [
            "day",
            "week",
            "month"
          ],
          "description": "The period for times_per_period. Optional."
        },
        "time_of_day": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "When during the day: ['morning'], ['morning','evening'], ['night'], etc. Optional."
        },
        "start_date": {
          "type": "string",
          "description": "Start date (YYYY-MM-DD). Required for add — default to today."
        },
        "end_date": {
          "type": "string",
          "description": "End date (YYYY-MM-DD). Set for 'end' action — default to today. Null means currently active."
        },
        "notes": {
          "type": "string",
          "description": "Free-text notes. Optional."
        }
      },
      "required": [
        "action"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_supplement_taken",
    "description": "Mark a medication or supplement as taken or not taken for a specific date. Only relevant when the user has daily tracking mode enabled. Use when the user says they took (or missed) a medication or supplement on a particular day.\n\nINFER — do not ask:\n- date: default to today\n- taken: default to true (marking as taken)\n\nSELECTOR — pass supplement_id if known, or supplement_name (case-insensitive substring) to resolve it. Exactly one required. If supplement_name matches more than one item, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Log supplement taken",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "supplement_id": {
          "type": "integer",
          "description": "Supplement ID. Alternative to supplement_name."
        },
        "supplement_name": {
          "type": "string",
          "description": "Alternative to supplement_id: name substring, case-insensitive (e.g. \"magnesium\")."
        },
        "date": {
          "type": "string",
          "description": "Date (YYYY-MM-DD). Default to today."
        },
        "taken": {
          "type": "boolean",
          "description": "true = taken, false = missed. Default: true."
        },
        "notes": {
          "type": "string",
          "description": "Optional note for this check-in."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_recovery_strategies",
    "description": "List the user's recovery and mindfulness strategies. Use when the user asks about their recovery practices, mindfulness routines, or you need strategy IDs before logging a session.\n\nINFER — do not ask:\n- filter: default to 'active'; use 'all' for history; use 'historical' for ended strategies only.\n\nReturns each strategy's id, name, category, schedule, start_date, and end_date.",
    "annotations": {
      "title": "List recovery strategies",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "filter": {
          "type": "string",
          "enum": [
            "active",
            "historical",
            "all"
          ],
          "description": "Which strategies to return. Default: 'active'."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "manage_recovery_strategy",
    "description": "Add, update, end, or delete a recovery/mindfulness strategy. Use when the user describes a new practice, changes a schedule, stops a practice, or removes one. Infer category from name, start_date defaults to today, infer schedule from context. ASK only if name is missing.\n\nSELECTOR for update/end/delete — pass id if known, or strategy_name (case-insensitive substring, e.g. \"sauna\") to resolve it. Exactly one of id or strategy_name required. If strategy_name matches more than one strategy, the call errors with candidate IDs to retry with.\n\nAFTER a successful 'add': do NOT just confirm and stop. Reply by (1) restating the assumed schedule (sessions per period, duration, time of day, start date) in plain language, and (2) asking the user to confirm or correct it — especially any optional fields you did NOT set (duration_minutes, time_of_day). Example: \"Logged sauna starting today, assuming once per week. Sound right? About how long do you usually go for, and what time of day — morning, evening?\" If the user corrects anything, call this tool again with action='update'. The goal is accurate adherence data, not a silent confirmation.",
    "annotations": {
      "title": "Manage recovery strategies",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "add",
            "update",
            "end",
            "delete"
          ],
          "description": "What to do. Required."
        },
        "id": {
          "type": "integer",
          "description": "Strategy ID. Required for update, end, delete unless strategy_name is given."
        },
        "strategy_name": {
          "type": "string",
          "description": "Alternative to id for update/end/delete: strategy name substring, case-insensitive (e.g. \"sauna\")."
        },
        "name": {
          "type": "string",
          "description": "Strategy name (e.g. 'Box Breathing'). Required for add; on update, sets a new name."
        },
        "category": {
          "type": "string",
          "enum": [
            "breathwork",
            "progressive_muscle_relaxation",
            "meditation",
            "cold_exposure",
            "heat_sauna",
            "contrast_therapy",
            "lymphatic_drainage",
            "stretching_mobility",
            "yoga",
            "journaling",
            "other"
          ],
          "description": "Category. Infer from name."
        },
        "sessions_per_period": {
          "type": "integer",
          "description": "Target sessions per period. Default: 1."
        },
        "period_unit": {
          "type": "string",
          "enum": [
            "day",
            "week"
          ],
          "description": "Period unit. Default: 'week'."
        },
        "duration_minutes": {
          "type": "integer",
          "description": "Target minutes per session. Optional."
        },
        "time_of_day": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "When during the day: ['morning'], ['evening'], etc. Optional."
        },
        "start_date": {
          "type": "string",
          "description": "Start date (YYYY-MM-DD). Default to today for add."
        },
        "end_date": {
          "type": "string",
          "description": "End date (YYYY-MM-DD). Default to today for end action."
        },
        "notes": {
          "type": "string",
          "description": "Free-text notes. Optional."
        }
      },
      "required": [
        "action"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_recovery_session",
    "description": "Log a completed or skipped recovery/mindfulness session. Use when the user says they did (or skipped) a breathing exercise, meditation, cold plunge, sauna, stretching, or any recovery practice. Also use for one-off standalone sessions not linked to a recurring strategy.\n\nINFER — do not ask:\n- date: default to today\n- category: infer from the practice name\n- strategy_name: use the strategy name if linked, or the user's description\n- duration_minutes: infer if mentioned (omit for skipped sessions)\n- quality: only include if the user rates it (1-5 scale)\n- skipped: true when the user says they skipped, missed, or didn't do a session; false (default) for completed sessions\n\nPREFERRED WORKFLOW: call list_recovery_strategies first to link the session to an active strategy for adherence tracking. If no matching strategy exists, log as standalone.",
    "annotations": {
      "title": "Log a recovery session",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "strategy_id": {
          "type": "integer",
          "description": "Strategy ID from list_recovery_strategies. Optional — omit for standalone sessions."
        },
        "date": {
          "type": "string",
          "description": "Date (YYYY-MM-DD). Default to today."
        },
        "category": {
          "type": "string",
          "enum": [
            "breathwork",
            "progressive_muscle_relaxation",
            "meditation",
            "cold_exposure",
            "heat_sauna",
            "contrast_therapy",
            "lymphatic_drainage",
            "stretching_mobility",
            "yoga",
            "journaling",
            "other"
          ],
          "description": "Category. Required."
        },
        "strategy_name": {
          "type": "string",
          "description": "Name of the practice. Required."
        },
        "duration_minutes": {
          "type": "integer",
          "description": "Duration in minutes. Optional — omit for skipped."
        },
        "quality": {
          "type": "integer",
          "description": "Subjective quality 1-5. Optional."
        },
        "skipped": {
          "type": "boolean",
          "description": "true if the session was skipped/missed. Default: false."
        },
        "notes": {
          "type": "string",
          "description": "Session notes or reason for skipping. Optional."
        }
      },
      "required": [
        "category",
        "strategy_name"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_recovery_sessions",
    "description": "List logged recovery sessions (completions and skips) within a date range, each with its ID. list_recovery_strategies only returns the recurring strategies (the schedule), never the individual logged entries against them — this is the only way to see those.\n\nupdate_recovery_session and delete_recovery_session can resolve a session on their own from session_date (+ optional session_category), so this is no longer required before either — use it to answer \"what recovery sessions have I logged\", audit/spot-check past entries (e.g. a sauna and a cold plunge logged separately on the same day that should have been one contrast_therapy entry), or get an id after an ambiguous update_recovery_session/delete_recovery_session match.\n\nINFER — do not ask:\n- start_date / end_date: default to the last 30 days. Widen the range yourself for an older lookup instead of asking the user for exact dates.\n- category: omit to return every category.\n\nMaximum range: 90 days per call. To audit or correct a longer history, make multiple sequential calls walking backwards (days 0-90, then 90-180, then 180-270...) until you have covered the period the user means. Don't stop after one call and report that as the whole history.",
    "annotations": {
      "title": "List recovery sessions",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Optional — defaults to 30 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Optional — defaults to today."
        },
        "category": {
          "type": "string",
          "enum": [
            "breathwork",
            "progressive_muscle_relaxation",
            "meditation",
            "cold_exposure",
            "heat_sauna",
            "contrast_therapy",
            "lymphatic_drainage",
            "stretching_mobility",
            "yoga",
            "journaling",
            "other"
          ],
          "description": "Optional — filter to one category. Omit to return every category."
        },
        "limit": {
          "type": "integer",
          "description": "Optional — max sessions to return. Default 50, max 200."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_recovery_session",
    "description": "Update one or more fields on an existing recovery session log entry. Use when the user wants to correct or change something already logged (e.g. wrong duration, quality rating, category, or notes). Only send the fields that need to change; omit all others.\n\nSELECTOR — pass id if known, or session_date (+ optional session_category to narrow) to resolve it. Exactly one of id or session_date required. If it matches more than one session, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Update a recovery session",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Recovery session ID. Alternative to session_date."
        },
        "session_date": {
          "type": "string",
          "description": "Alternative to id: the date (YYYY-MM-DD) the session was logged on. Optionally narrow with session_category."
        },
        "session_category": {
          "type": "string",
          "enum": [
            "breathwork",
            "progressive_muscle_relaxation",
            "meditation",
            "cold_exposure",
            "heat_sauna",
            "contrast_therapy",
            "lymphatic_drainage",
            "stretching_mobility",
            "yoga",
            "journaling",
            "other"
          ],
          "description": "Optional, narrows session_date to one category when more than one session shares that date. Ignored when id is given."
        },
        "date": {
          "type": "string",
          "description": "Updated date (YYYY-MM-DD). Optional."
        },
        "category": {
          "type": "string",
          "enum": [
            "breathwork",
            "progressive_muscle_relaxation",
            "meditation",
            "cold_exposure",
            "heat_sauna",
            "contrast_therapy",
            "lymphatic_drainage",
            "stretching_mobility",
            "yoga",
            "journaling",
            "other"
          ],
          "description": "Updated category. Optional."
        },
        "strategy_name": {
          "type": "string",
          "description": "Updated practice name. Optional."
        },
        "duration_minutes": {
          "type": "integer",
          "description": "Updated duration in minutes. Optional."
        },
        "quality": {
          "type": "integer",
          "description": "Updated quality 1-5. Optional."
        },
        "skipped": {
          "type": "boolean",
          "description": "Updated skipped status. Optional."
        },
        "notes": {
          "type": "string",
          "description": "Updated notes. Optional."
        },
        "strategy_id": {
          "type": "integer",
          "description": "Updated strategy ID. Optional — set null to unlink."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_recovery_session",
    "description": "Permanently delete a recovery session log entry. This action is irreversible. If the user's intent is ambiguous, ask which session to remove.\n\nSELECTOR — pass id if known, or session_date (+ optional session_category to narrow) to resolve it. Exactly one of id or session_date required. If it matches more than one session, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Delete a recovery session",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "description": "Recovery session ID. Alternative to session_date."
        },
        "session_date": {
          "type": "string",
          "description": "Alternative to id: the date (YYYY-MM-DD) the session was logged on. Optionally narrow with session_category."
        },
        "session_category": {
          "type": "string",
          "enum": [
            "breathwork",
            "progressive_muscle_relaxation",
            "meditation",
            "cold_exposure",
            "heat_sauna",
            "contrast_therapy",
            "lymphatic_drainage",
            "stretching_mobility",
            "yoga",
            "journaling",
            "other"
          ],
          "description": "Optional, narrows session_date to one category when more than one session shares that date. Ignored when id is given."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_run",
    "description": "UNIT INPUTS: never convert units yourself. Pass the user's number for a _lb / _mi / _in field exactly as stated, and when they gave kg / km / cm set input_weight_unit / input_distance_unit / input_length_unit in that same object. Omit the companion when the number is already lb / mi / in. The tool converts once before storage, so this overrides any wording that asks you to do the arithmetic.\n\nCreate an editable running card. Use for a completed run OR a future run plan.\n\nintent:\n- log (default): the run happened. date, distance_mi and duration_sec are required. This writes the completed run and returns a card marker for in-app editing.\n- plan: the run has NOT happened yet. Create a planned run card. Never put a future run in completed history.\n\nRUN TYPE: distinguish easy, long, tempo, interval, recovery, race, fartlek, threshold, progression and hills when the runner or workout structure supports it. If a wearable run is unspecified, do NOT call it easy merely because it was a run.\n\nDETAIL: preserve elapsed time, HR, cadence, power, elevation, RPE, splits and structured segments only when supplied by the user/source. Never invent sensor data or splits.",
    "annotations": {
      "title": "Run card",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "intent": {
          "type": "string",
          "enum": [
            "log",
            "plan"
          ],
          "description": "log = completed run; plan = future run. Elias uses plan for a workout the runner has not done yet."
        },
        "date": {
          "type": "string",
          "description": "YYYY-MM-DD. Completed runs default to today when context allows; planned runs use the intended date when known."
        },
        "start_time": {
          "type": "string",
          "description": "Local HH:MM only when known."
        },
        "distance_mi": {
          "type": "number",
          "description": "Distance. In mi, or km with input_distance_unit set. See UNIT INPUTS."
        },
        "duration_sec": {
          "type": "integer",
          "description": "Moving/workout duration in seconds."
        },
        "elapsed_sec": {
          "type": "integer",
          "description": "Wall-clock elapsed time including pauses, only when known."
        },
        "run_type": {
          "type": "string",
          "enum": [
            "easy",
            "long",
            "tempo",
            "interval",
            "recovery",
            "race",
            "fartlek",
            "threshold",
            "progression",
            "hills",
            "other"
          ],
          "description": "Classify only when the runner or workout structure supports it. Do not turn an unspecified wearable run into easy."
        },
        "surface": {
          "type": "string",
          "enum": [
            "road",
            "trail",
            "track",
            "treadmill",
            "other"
          ]
        },
        "avg_hr": {
          "type": "integer",
          "description": "Average heart rate only when known."
        },
        "avg_cadence_spm": {
          "type": "number",
          "description": "Average running cadence in steps/min only when known."
        },
        "avg_power_w": {
          "type": "number",
          "description": "Average running power in watts only when known."
        },
        "elevation_gain_m": {
          "type": "number",
          "description": "Elevation gain in metres only when known."
        },
        "rpe": {
          "type": "number",
          "minimum": 1,
          "maximum": 10,
          "description": "RPE 1-10. For a plan this is target RPE."
        },
        "perceived_effort": {
          "type": "string",
          "enum": [
            "too_easy",
            "about_right",
            "too_hard"
          ],
          "description": "Post-run check-in only."
        },
        "pain_notes": {
          "type": "string",
          "description": "Pain/discomfort exactly as the runner described it."
        },
        "notes": {
          "type": "string"
        },
        "splits": {
          "type": "array",
          "maxItems": 100,
          "description": "Actual splits/laps only when supplied by the runner/source. Never invent.",
          "items": {
            "type": "object",
            "properties": {
              "index": {
                "type": "integer"
              },
              "distance_mi": {
                "type": "number",
                "description": "In mi, or km with input_distance_unit set. See UNIT INPUTS."
              },
              "duration_sec": {
                "type": "integer"
              },
              "avg_hr": {
                "type": "integer"
              },
              "avg_cadence_spm": {
                "type": "number"
              },
              "avg_power_w": {
                "type": "number"
              },
              "elevation_change_m": {
                "type": "number"
              },
              "input_distance_unit": {
                "type": "string",
                "enum": [
                  "mi",
                  "km"
                ],
                "description": "Set to km when the user gave km for the _mi fields in this object. Omit when they are already mi."
              }
            },
            "required": [
              "index"
            ]
          }
        },
        "segments": {
          "type": "array",
          "maxItems": 50,
          "description": "Workout structure, e.g. warmup, 6x800m threshold, recovery, cooldown. This may describe a plan or what was actually performed.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "warmup",
                  "easy",
                  "steady",
                  "tempo",
                  "threshold",
                  "interval",
                  "recovery",
                  "hill",
                  "cooldown",
                  "other"
                ]
              },
              "repeats": {
                "type": "integer"
              },
              "distance_mi": {
                "type": "number",
                "description": "In mi, or km with input_distance_unit set. See UNIT INPUTS."
              },
              "duration_sec": {
                "type": "integer"
              },
              "target_pace_sec_per_mi": {
                "type": "number",
                "description": "In mi, or km with input_distance_unit set. See UNIT INPUTS."
              },
              "target_hr_min": {
                "type": "integer"
              },
              "target_hr_max": {
                "type": "integer"
              },
              "notes": {
                "type": "string"
              },
              "input_distance_unit": {
                "type": "string",
                "enum": [
                  "mi",
                  "km"
                ],
                "description": "Set to km when the user gave km for the _mi fields in this object. Omit when they are already mi."
              }
            },
            "required": [
              "name",
              "kind"
            ]
          }
        },
        "input_distance_unit": {
          "type": "string",
          "enum": [
            "mi",
            "km"
          ],
          "description": "Set to km when the user gave km for the _mi fields in this object. Omit when they are already mi."
        }
      },
      "required": [
        "intent"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_runs",
    "description": "List runs within a date range. Returns a compact Runner State summary first, then individual runs. This is the preferred single read for Elias: current-week volume, four-week baseline, longest run, confirmed easy/quality mix, unclassified mileage, pace trend, heart points and available HR are computed together rather than pieced together from several calls.\n\nMaximum range: 92 days. Defaults to 7 days ago through today. For a longer span, make several calls covering consecutive windows.",
    "annotations": {
      "title": "List runs",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Optional — defaults to 7 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Optional — defaults to today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_run",
    "description": "UNIT INPUTS: never convert units yourself. Pass the user's number for a _lb / _mi / _in field exactly as stated, and when they gave kg / km / cm set input_weight_unit / input_distance_unit / input_length_unit in that same object. Omit the companion when the number is already lb / mi / in. The tool converts once before storage, so this overrides any wording that asks you to do the arithmetic.\n\nDelete a run. Use when the user wants to remove a run entry. ASK for confirmation if the user's intent is ambiguous.\n\nSELECTOR — pass id if known, or date (+ optional distance_mi to narrow, matched approximately within 0.25 mi) to resolve it. Exactly one of id or date required. If it matches more than one run, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Delete a run",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Run UUID. Alternative to date."
        },
        "date": {
          "type": "string",
          "description": "Alternative to id: the date (YYYY-MM-DD) the run was logged on. Optionally narrow with distance_mi."
        },
        "distance_mi": {
          "type": "number",
          "description": "Optional, narrows date to a run within 0.25 mi of this value when more than one run shares that date. Ignored when id is given. In mi, or km with input_distance_unit set. See UNIT INPUTS."
        },
        "input_distance_unit": {
          "type": "string",
          "enum": [
            "mi",
            "km"
          ],
          "description": "Set to km when the user gave km for the _mi fields in this object. Omit when they are already mi."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_rest_day",
    "description": "Mark a date as an intentional rest day. Use when the user says they took a rest day, are taking one today, or wants to mark a past day as rest after the fact (e.g. \"this past Monday was a rest day\", \"today is a rest day\", \"I rested yesterday\"). Suppresses the workout-prompt nudges for that date and lets the engine treat the day as planned, not skipped.\n\nINFER — do not ask:\n- date: parse the user's reference. \"today\" = today, \"yesterday\" = today minus 1, \"last Monday\" = the most recent Monday before today, etc. Default to today when nothing is specified.\n\nIdempotent — calling twice on the same date is a no-op (the second call returns the same confirmation). To remove a rest day, use cancel_rest_day.",
    "annotations": {
      "title": "Log a rest day",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "The date to mark as a rest day. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "cancel_rest_day",
    "description": "Remove a previously declared rest day. Use when the user changes their mind (\"scratch that, I'm going to lift today after all\") or wants to undo a mistaken declaration.\n\nINFER — do not ask:\n- date: parse the user's reference; default to today.",
    "annotations": {
      "title": "Cancel a rest day",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "The date to un-mark. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_rest_days",
    "description": "List the dates a user has marked as rest days within a range. Use when the user asks about their rest pattern (\"how many rest days have I taken this month?\", \"did I rest last week?\").\n\nINFER — do not ask:\n- start_date: default to 30 days ago\n- end_date: default to today",
    "annotations": {
      "title": "List rest days",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Default: 30 days ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_goals",
    "description": "Analyze or show the user's current and past goals. Returns active/paused formal goals, completed/historical formal goals, and current standard targets.\n\nUse this when the user asks what goals they have or asks to review/analyze their goals. Pass include_capabilities: true ONLY when the user asks what kinds of goals Wellness Project supports; it appends the full catalog of goal types and their inputs, which is large. Do not call this tool merely to obtain an ID before create_goal or update_goal; those write tools resolve current goals themselves.",
    "annotations": {
      "title": "List goals",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "include_capabilities": {
          "type": "boolean",
          "description": "Append the supported goal types and their inputs. Default false. Only set this when the user is asking what kinds of goals the app supports."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "create_goal",
    "description": "UNIT INPUTS: never convert units yourself. Pass the user's number for a _lb / _mi / _in field exactly as stated, and when they gave kg / km / cm set input_weight_unit / input_distance_unit / input_length_unit in that same object. Omit the companion when the number is already lb / mi / in. The tool converts once before storage, so this overrides any wording that asks you to do the arithmetic.\n\nCreate a new Wellness Project goal or standard target. Call this directly when the user wants to establish a goal; there is no schema-discovery or list_goals prerequisite. Available goal types and inputs come from the canonical Goals definitions.\n\nThis tool loads the user's current goals itself before writing, so do not call list_goals first: it reports what a standard target changed from, and refuses to stack a second goal on top of one that already covers the same thing, naming that goal's ID to use with update_goal. Infer the goal_type and canonical inputs from the request. Standard targets use target_value. Formal goal fields are described on the generated inputs.",
    "annotations": {
      "title": "Create goal",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "goal_type": {
          "type": "string",
          "enum": [
            "weight_loss",
            "strength",
            "consistency",
            "body_comp",
            "race",
            "n1_experiment",
            "daily_calories",
            "daily_protein_g",
            "daily_fat_g",
            "daily_carbs_g",
            "weekly_workouts",
            "daily_steps",
            "weekly_azm_target",
            "sleep_hours_target",
            "body_weight_target_lb",
            "body_fat_pct_target"
          ],
          "description": "Goal type to create."
        },
        "title": {
          "type": "string",
          "description": "Concise title, inferred from the goal inputs."
        },
        "start_date": {
          "type": "string",
          "description": "YYYY-MM-DD. Default: today."
        },
        "acknowledged_warnings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Warning keys the user explicitly acknowledged after a guarded create attempt. Omit otherwise."
        },
        "start_weight_lb": {
          "type": "number",
          "description": "For weight_loss. Starting body weight. Required on create. Set at create, not editable later. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 1
        },
        "target_weight_lb": {
          "type": "number",
          "description": "For weight_loss. Target body weight. Required on create. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 1
        },
        "weeks": {
          "type": "integer",
          "description": "For weight_loss. Duration in weeks when target_date is not supplied. For body_comp. Duration in weeks when target_date is not supplied.",
          "minimum": 4,
          "maximum": 104
        },
        "target_date": {
          "type": "string",
          "description": "For weight_loss. Target date in YYYY-MM-DD format. Use this when the user names a deadline. For body_comp. Target date in YYYY-MM-DD format. Use this when the user names a deadline."
        },
        "exercise_name": {
          "type": "string",
          "description": "For strength. Exercise name. Required on create. Set at create, not editable later."
        },
        "start_1rm_lb": {
          "type": "number",
          "description": "For strength. Estimated 1RM when the goal starts. Required on create. Set at create, not editable later. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 0
        },
        "target_1rm_lb": {
          "type": "number",
          "description": "For strength. Target 1RM. Required on create. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 0
        },
        "metric": {
          "type": "string",
          "description": "For consistency. What consistency behavior to track. Required on create. Set at create, not editable later. For body_comp. Body composition metric. Required on create. Set at create, not editable later. For nutrition. Legacy nutrition metric.",
          "enum": [
            "workouts",
            "calories_logged",
            "weigh_ins",
            "sleep_duration",
            "body_fat_pct",
            "lean_mass_lb",
            "daily_calories",
            "daily_protein_g",
            "daily_carbs_g",
            "daily_fat_g"
          ]
        },
        "target_per_week": {
          "type": "integer",
          "description": "For consistency. Target occurrences per week. Required on create.",
          "minimum": 1,
          "maximum": 7
        },
        "target_hours": {
          "type": "number",
          "description": "For consistency. Nightly sleep target in hours. Required when metric is sleep_duration.",
          "minimum": 0,
          "maximum": 24
        },
        "week_window": {
          "type": "string",
          "description": "For consistency. How the week this goal is measured against is bounded: rolling = the last 7 days, sunday/monday = a calendar week that resets on that day. Default: rolling.",
          "enum": [
            "rolling",
            "sunday",
            "monday"
          ]
        },
        "start_value": {
          "type": "number",
          "description": "For body_comp. Starting value when the goal begins. Required on create. Set at create, not editable later."
        },
        "target_value": {
          "type": "number",
          "description": "For body_comp. Target body composition value. Required on create. For nutrition. Legacy nutrition target value. For standard targets, this is the numeric target value."
        },
        "target_distance_mi": {
          "type": "number",
          "description": "For race. Target race distance. Required on create. In mi, or km with input_distance_unit set. See UNIT INPUTS.",
          "minimum": 0
        },
        "target_time_sec": {
          "type": "integer",
          "description": "For race. Target finish time in seconds. Required on create.",
          "minimum": 1
        },
        "race_date": {
          "type": "string",
          "description": "For race. Race date in YYYY-MM-DD format. Required on create."
        },
        "supplement_id": {
          "type": "integer",
          "description": "For n1_experiment. Existing supplement ID. Use either supplement_id or the new-supplement fields."
        },
        "new_name": {
          "type": "string",
          "description": "For n1_experiment. Name for a new supplement when not using supplement_id."
        },
        "new_brand": {
          "type": "string",
          "description": "For n1_experiment. Optional brand for a new supplement."
        },
        "new_dose_amount": {
          "type": "number",
          "description": "For n1_experiment. Dose amount for a new supplement."
        },
        "new_dose_unit": {
          "type": "string",
          "description": "For n1_experiment. Dose unit for a new supplement.",
          "enum": [
            "mg",
            "g",
            "mcg",
            "IU",
            "ml",
            "capsule",
            "tablet",
            "scoop"
          ]
        },
        "baseline_direction": {
          "type": "string",
          "description": "For n1_experiment. Use already logged previous 14 days or collect the next 14 days.",
          "enum": [
            "past",
            "future"
          ]
        },
        "intervention_days": {
          "type": "integer",
          "description": "For n1_experiment. Intervention duration in days.",
          "minimum": 14,
          "maximum": 120
        },
        "input_weight_unit": {
          "type": "string",
          "enum": [
            "lb",
            "kg"
          ],
          "description": "Set to kg when the user gave kg for the _lb fields in this object. Omit when they are already lb."
        },
        "input_distance_unit": {
          "type": "string",
          "enum": [
            "mi",
            "km"
          ],
          "description": "Set to km when the user gave km for the _mi fields in this object. Omit when they are already mi."
        }
      },
      "required": [
        "goal_type"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_goal",
    "description": "UNIT INPUTS: never convert units yourself. Pass the user's number for a _lb / _mi / _in field exactly as stated, and when they gave kg / km / cm set input_weight_unit / input_distance_unit / input_length_unit in that same object. Omit the companion when the number is already lb / mi / in. The tool converts once before storage, so this overrides any wording that asks you to do the arithmetic.\n\nChange, complete, pause, stop/end, reopen, or delete an existing goal. Call this tool directly for ordinary goal changes. It already loads the user's current goals and resolves a unique goal from goal_id or a natural-language goal_ref, so do NOT call list_goals first just to find an ID.\n\nFor edit, pass only the fields to change. For complete/pause/end/reopen/delete, no edit fields are required. If goal_ref genuinely matches multiple goals, this tool returns the candidates and changes nothing. Formal goal end/delete follows the existing Goals UI cancel lifecycle; standard-target end/pause turns that target off while delete removes it.",
    "annotations": {
      "title": "Update goal",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "edit",
            "complete",
            "pause",
            "end",
            "reopen",
            "delete"
          ],
          "description": "What to do. 'end' when the user is stopping or canceling a goal; for a formal goal that follows the app's cancel behavior and removes it."
        },
        "goal_id": {
          "type": "integer",
          "description": "Formal goal ID, when known."
        },
        "goal_ref": {
          "type": "string",
          "description": "Natural-language reference when no ID is known, e.g. \"protein\", \"10K\", \"weight loss\". This tool resolves it against current goals itself."
        },
        "title": {
          "type": "string",
          "description": "Concise title, inferred from the goal inputs."
        },
        "start_date": {
          "type": "string",
          "description": "YYYY-MM-DD. Default: today."
        },
        "acknowledged_warnings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Warning keys the user explicitly acknowledged after a guarded create attempt. Omit otherwise."
        },
        "start_weight_lb": {
          "type": "number",
          "description": "For weight_loss. Starting body weight. Required on create. Set at create, not editable later. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 1
        },
        "target_weight_lb": {
          "type": "number",
          "description": "For weight_loss. Target body weight. Required on create. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 1
        },
        "weeks": {
          "type": "integer",
          "description": "For weight_loss. Duration in weeks when target_date is not supplied. For body_comp. Duration in weeks when target_date is not supplied.",
          "minimum": 4,
          "maximum": 104
        },
        "target_date": {
          "type": "string",
          "description": "For weight_loss. Target date in YYYY-MM-DD format. Use this when the user names a deadline. For body_comp. Target date in YYYY-MM-DD format. Use this when the user names a deadline."
        },
        "exercise_name": {
          "type": "string",
          "description": "For strength. Exercise name. Required on create. Set at create, not editable later."
        },
        "start_1rm_lb": {
          "type": "number",
          "description": "For strength. Estimated 1RM when the goal starts. Required on create. Set at create, not editable later. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 0
        },
        "target_1rm_lb": {
          "type": "number",
          "description": "For strength. Target 1RM. Required on create. In lb, or kg with input_weight_unit set. See UNIT INPUTS.",
          "minimum": 0
        },
        "metric": {
          "type": "string",
          "description": "For consistency. What consistency behavior to track. Required on create. Set at create, not editable later. For body_comp. Body composition metric. Required on create. Set at create, not editable later. For nutrition. Legacy nutrition metric.",
          "enum": [
            "workouts",
            "calories_logged",
            "weigh_ins",
            "sleep_duration",
            "body_fat_pct",
            "lean_mass_lb",
            "daily_calories",
            "daily_protein_g",
            "daily_carbs_g",
            "daily_fat_g"
          ]
        },
        "target_per_week": {
          "type": "integer",
          "description": "For consistency. Target occurrences per week. Required on create.",
          "minimum": 1,
          "maximum": 7
        },
        "target_hours": {
          "type": "number",
          "description": "For consistency. Nightly sleep target in hours. Required when metric is sleep_duration.",
          "minimum": 0,
          "maximum": 24
        },
        "week_window": {
          "type": "string",
          "description": "For consistency. How the week this goal is measured against is bounded: rolling = the last 7 days, sunday/monday = a calendar week that resets on that day. Default: rolling.",
          "enum": [
            "rolling",
            "sunday",
            "monday"
          ]
        },
        "start_value": {
          "type": "number",
          "description": "For body_comp. Starting value when the goal begins. Required on create. Set at create, not editable later."
        },
        "target_value": {
          "type": "number",
          "description": "For body_comp. Target body composition value. Required on create. For nutrition. Legacy nutrition target value. For standard targets, this is the numeric target value."
        },
        "target_distance_mi": {
          "type": "number",
          "description": "For race. Target race distance. Required on create. In mi, or km with input_distance_unit set. See UNIT INPUTS.",
          "minimum": 0
        },
        "target_time_sec": {
          "type": "integer",
          "description": "For race. Target finish time in seconds. Required on create.",
          "minimum": 1
        },
        "race_date": {
          "type": "string",
          "description": "For race. Race date in YYYY-MM-DD format. Required on create."
        },
        "supplement_id": {
          "type": "integer",
          "description": "For n1_experiment. Existing supplement ID. Use either supplement_id or the new-supplement fields."
        },
        "new_name": {
          "type": "string",
          "description": "For n1_experiment. Name for a new supplement when not using supplement_id."
        },
        "new_brand": {
          "type": "string",
          "description": "For n1_experiment. Optional brand for a new supplement."
        },
        "new_dose_amount": {
          "type": "number",
          "description": "For n1_experiment. Dose amount for a new supplement."
        },
        "new_dose_unit": {
          "type": "string",
          "description": "For n1_experiment. Dose unit for a new supplement.",
          "enum": [
            "mg",
            "g",
            "mcg",
            "IU",
            "ml",
            "capsule",
            "tablet",
            "scoop"
          ]
        },
        "baseline_direction": {
          "type": "string",
          "description": "For n1_experiment. Use already logged previous 14 days or collect the next 14 days.",
          "enum": [
            "past",
            "future"
          ]
        },
        "intervention_days": {
          "type": "integer",
          "description": "For n1_experiment. Intervention duration in days.",
          "minimum": 14,
          "maximum": 120
        },
        "input_weight_unit": {
          "type": "string",
          "enum": [
            "lb",
            "kg"
          ],
          "description": "Set to kg when the user gave kg for the _lb fields in this object. Omit when they are already lb."
        },
        "input_distance_unit": {
          "type": "string",
          "enum": [
            "mi",
            "km"
          ],
          "description": "Set to km when the user gave km for the _mi fields in this object. Omit when they are already mi."
        }
      },
      "required": [
        "action"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_blog_posts",
    "description": "Search the public Crew Blog at /blog for advisor-authored daily posts. Only call when the user explicitly asks about the blog or what an advisor has written; don't volunteer posts in normal conversation.\n\nReturns each matching post's slug, title, summary, advisor name, and date. Link a post inline as /blog/<slug>.",
    "annotations": {
      "title": "List blog posts",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "advisor_slug": {
          "type": "string",
          "enum": [
            "fitness-helper",
            "coach",
            "physio",
            "nutritionist",
            "longevity",
            "biohacker",
            "bodybuilder",
            "runner",
            "protocol"
          ],
          "description": "Optional — filter to one advisor (e.g. \"nutritionist\" for Casey Mills)."
        },
        "query": {
          "type": "string",
          "description": "Optional — substring filter applied to title and summary (case-insensitive)."
        },
        "limit": {
          "type": "integer",
          "description": "Optional — number of posts to return. Default 10, max 30."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "get_app_guide_section",
    "description": "Look up general customer help for the Wellness Project app. Use this only for broad questions about where a feature lives, how to use it, what a visible feature does, common troubleshooting steps, or pricing.\n\nThe returned guide is intentionally limited to stable, customer-facing help. Do not add or infer implementation details, technical architecture, policy or legal interpretation, privacy or security claims beyond the exact returned text, billing mechanics, release or version history, internal timing or targeting rules, or other details that are not in the returned section.\n\nFor navigation, the guide result is the source of truth. Navigation grounding: use only app sections, routes, labels, buttons, gestures, and menu paths explicitly named in this guide result. Do not infer, invent, or autocomplete nearby navigation. If the requested path or action is not confirmed here, say you are not sure where it lives rather than guessing.\n\nFor cost, price, Free vs Pro, Founding Member, upgrading, or the 3-analysis limit, use topic=pricing. That result consists of the two approved product messages plus the subscription page link. Reproduce exactly one appropriate message verbatim, include the supplied subscription link, and add no other pricing detail. Use the Founding Member message for ordinary pricing questions and the exhausted message only when the user says they already used the daily analysis allowance. In the in-app coach only, append this CTA block exactly after the chosen message:\n[CHIPS]\nGet Pro Now|open_paywall\n[/CHIPS]\n\nExamples: \"where do I see my PRs?\" -> pages_training; \"what does Fit Score include?\" -> pages_dashboard; \"how do I connect Oura?\" -> wearables; \"how do I set a calorie goal?\" -> goals; \"how do I log a meal?\" -> logging; \"how much does it cost?\" -> pricing.",
    "annotations": {
      "title": "App guide lookup",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "topic": {
          "type": "string",
          "enum": [
            "pages_dashboard",
            "pages_training",
            "pages_nutrition",
            "personas",
            "logging",
            "photos",
            "wearables",
            "goals",
            "challenges",
            "privacy",
            "troubleshooting",
            "pricing"
          ],
          "description": "Choose one general help area. pages_dashboard = Dashboard, Me, Settings, Fit Score, Insights. pages_training = Fitness, Train, PRs, running, heart, recovery, injuries. pages_nutrition = Fuel, Nutrition, sleep, body, wellbeing, cycle, labs. personas = AI specialists. logging = ways to log or edit data. photos = meal photos, labels, barcodes. wearables = connected health sources and AI assistants. goals = goals and standard targets. challenges = friend challenges. privacy = account controls and links to posted policies only. troubleshooting = common navigation and recovery steps. pricing = the two approved product pricing messages plus the subscription link."
        }
      },
      "required": [
        "topic"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "log_cycle",
    "description": "Log a period to the user's cycle log. Handles all cases:\n\n- Starting a period today: \"my period started today\"\n- Backfilling a past period: \"my period started May 3rd and ended May 8th\"\n- Resuming a period ended today: \"actually I'm still on my period\" — detects that today's period was marked ended and reopens it\n- Logging just a start with no end yet: \"I just got my period\"\n\nBefore logging, check that cycle tracking is enabled (cycle_prefs.tracking_enabled AND consented_at, both required -- a user can have consented in the past and later turned tracking off). If not, tell the user to turn it on from the dashboard first.\n\nINFER — do not ask:\n- started_on: default to today for current-period statements\n- ended_on: omit unless the user says it ended; infer from context (\"5-day period starting May 3\" → ended_on May 7)\n\nDo NOT use this tool to log future dates.",
    "annotations": {
      "title": "Log a period",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "started_on": {
          "type": "string",
          "description": "Period start date. Format: YYYY-MM-DD. Required."
        },
        "ended_on": {
          "type": "string",
          "description": "Period end date. Format: YYYY-MM-DD. Omit if period is still active."
        }
      },
      "required": [
        "started_on"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "list_cycle",
    "description": "List the user's logged period records within a date range. update_cycle and delete_cycle can resolve a record on their own from date, so this is no longer required before either — use it when the user asks about their cycle history, or get an id after an ambiguous update_cycle/delete_cycle match.\n\nINFER — do not ask:\n- start_date: default to 6 months ago\n- end_date: default to today",
    "annotations": {
      "title": "List cycle records",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "start_date": {
          "type": "string",
          "description": "Start of date range. Format: YYYY-MM-DD. Default: 6 months ago."
        },
        "end_date": {
          "type": "string",
          "description": "End of date range. Format: YYYY-MM-DD. Default: today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "update_cycle",
    "description": "Update an existing period record. Use to correct dates, add a missing end date, or clear an end date (resume).\n\nCommon uses:\n- \"my period ended on the 9th not the 8th\" → update ended_on\n- \"actually my period started the 2nd not the 3rd\" → update started_on\n- \"I'm still on my period\" → clear ended_on (pass null) to reopen it\n\nSELECTOR — pass id if known, or date (the period's start date, or any date that falls within it) to resolve it. Exactly one required. If date matches more than one record, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Update a period record",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Record ID. Alternative to date."
        },
        "date": {
          "type": "string",
          "description": "Alternative to id: a date (YYYY-MM-DD) that identifies the period — its start date, or any day within it. Resolves only when exactly one record matches."
        },
        "started_on": {
          "type": "string",
          "description": "Updated start date. Format: YYYY-MM-DD. Optional."
        },
        "ended_on": {
          "type": "string",
          "description": "Updated end date. Format: YYYY-MM-DD. Pass null to clear (mark active). Optional."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "delete_cycle",
    "description": "Delete a period record. Only delete if the user explicitly asks to remove a specific record. Do not delete to \"fix\" a record — use update_cycle instead.\n\nSELECTOR — pass id if known, or date (the period's start date, or any date that falls within it) to resolve it. Exactly one required. If date matches more than one record, the call errors with candidate IDs to retry with.",
    "annotations": {
      "title": "Delete a period record",
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Record ID. Alternative to date."
        },
        "date": {
          "type": "string",
          "description": "Alternative to id: a date (YYYY-MM-DD) that identifies the period — its start date, or any day within it. Resolves only when exactly one record matches."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "show_week_workouts",
    "description": "Show the user their past 7 days of workouts (heart points per day, days trained) as an inline chart. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "This week in training",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 7d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_week_fit_score",
    "description": "Show the user their current Fit Score (the app's 0-100 composite), how it breaks down across its six components (Train, Fuel, Sleep, Recovery, Steps, Wellbeing), and the past 7 days of daily scores, as one inline card. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "This week in Fit Score",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 7d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_week_macros",
    "description": "Show the user their past 7 days of calories and macros (protein, carbs, fat) vs targets as an inline chart. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "This week in nutrition",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 7d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_week_sleep",
    "description": "Show the user their past 7 days of sleep (hours per night and sleep score) as an inline chart. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "This week in sleep",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 7d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_week_steps",
    "description": "Show the user their past 7 days of step counts vs their daily goal as an inline chart. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "This week in steps",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 7d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_body_weight",
    "description": "Show the user their body weight (and body-fat %) trend over time as an interactive line chart with a 30/90-day/1-year range toggle. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Body weight trend",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_recovery",
    "description": "Show the user their resting heart rate and HRV recovery trend over time as an interactive dual-line chart with a 30/90-day/1-year range toggle. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Recovery - RHR & HRV",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_runs",
    "description": "Show the user their running mileage over the last 14 days as an inline bar chart. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Runs - last 2 weeks",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 30d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_exercise_progression",
    "description": "Show the user's estimated 1-rep-max progression for a lift over time as an interactive line chart, with filters for date range and muscle group, and an exercise picker. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Exercise progression",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "exercise": {
          "type": "string",
          "description": "Exercise name to chart (e.g. \"Bench Press\"). Optional; defaults to the most-logged lift in the window."
        },
        "muscle_group": {
          "type": "string",
          "description": "Optional muscle-group filter for the exercise picker (e.g. \"Chest\", \"Legs\")."
        },
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_workout",
    "description": "Show a single logged workout session's exercises and sets as an inline card. Defaults to the most recent workout; can target a specific date. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Workout detail",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "Workout date (YYYY-MM-DD). Optional; defaults to the most recent workout."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_health_overview",
    "description": "Show a rich overview of wearable health signals including steps, Zone Minutes, resting heart rate, HRV, VO2max, and stress. Prefer this for broad wearable or overall health-trend questions. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Health overview",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 30d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_sleep_detail",
    "description": "Show one night of sleep in detail with duration, score, stages, bedtime, wake time, awakenings, and recent-night context. Prefer this for last-night or specific-night sleep questions. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Sleep detail",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "Night ending date (YYYY-MM-DD). Optional; defaults to the most recent sleep entry."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_body_composition",
    "description": "Show body composition over time with an interactive metric picker for weight, body fat, lean and muscle mass, hydration, visceral fat, BMI, waist, and related scale metrics. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Body composition",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 90d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_meal_diary",
    "description": "Show the meals logged on a day as a rich diary with daily calories and macros versus targets. Prefer this for what-did-I-eat and daily food-log review questions. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Meal diary",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "Diary date (YYYY-MM-DD). Optional; defaults to today."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "show_wellbeing",
    "description": "Show energy, mood, stress, and soreness together with recent context and an overall wellbeing trend. Prefer this for how-I-have-been-feeling and subjective recovery questions. When the user asks about this, prefer calling this tool and rendering the interactive MCP app over describing the underlying rows in text. Returns a short text summary alongside the visual view.",
    "annotations": {
      "title": "Wellbeing",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "range": {
          "type": "string",
          "enum": [
            "7d",
            "30d",
            "90d",
            "1y"
          ],
          "description": "Time window. One of 7d, 30d, 90d, 1y. Default 30d."
        }
      }
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "description": "Widget discriminant identifying the payload shape."
        },
        "generatedAt": {
          "type": "string",
          "description": "ISO timestamp the snapshot was built."
        }
      },
      "required": [
        "kind"
      ]
    }
  },
  {
    "name": "list_personal_context",
    "description": "List the user's active Personal Context memories: durable circumstances and preferences remembered across conversations (e.g. travels most weeks, gym has no squat rack, trains early mornings, wants blunt feedback). Use when the user asks what has been remembered about them, or before proposing a new memory to check whether an existing one already covers the subject. Read-only.",
    "annotations": {
      "title": "List Personal Context",
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {}
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  },
  {
    "name": "add_or_update_personal_context",
    "description": "Add a new Personal Context memory, or update an existing one by id. A memory is a durable circumstance or preference that should carry across future unrelated conversations (e.g. \"travels most weeks\", \"gym has no squat rack\", \"prefers short home workouts\", \"wants blunt feedback\"). Use list_personal_context first to check whether an existing memory already covers the subject, and pass its id with operation update rather than creating a duplicate.\n\nHealth history does not belong here. Injuries, lab results, meals, workouts, sleep and body metrics each have their own dedicated tools that store them as structured data the app can chart and reason over; writing any of them as a memory duplicates that record and degrades it to loose text.\n\nThis writes immediately with no separate approval step. Free accounts are capped at 3 memories and Pro accounts at 50; updating an existing memory by id is always allowed even at the cap. There is no delete or bulk-write capability here.",
    "annotations": {
      "title": "Add or update Personal Context",
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "inputSchema": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "enum": [
            "add",
            "update"
          ],
          "description": "add creates a new memory. update replaces the content of an existing one, identified by memory_id."
        },
        "memory_id": {
          "type": "integer",
          "minimum": 1,
          "description": "Required for update. The numeric id from list_personal_context. Omit for add."
        },
        "content": {
          "type": "string",
          "description": "The memory text: a compact, durable fact or preference with no conversational filler."
        }
      },
      "required": [
        "operation",
        "content"
      ]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Human-readable result text returned by the tool."
        }
      },
      "required": [
        "result"
      ]
    }
  }
]
